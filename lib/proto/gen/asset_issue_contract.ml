(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/asset_issue_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct end

(**/**)

module rec Protocol : sig
  module rec AssetIssueContract : sig
    module rec FrozenSupply : sig
      type t = {
        frozen_amount : int64;  (** {%html:
<p>asset amount</p>
%} *)
        frozen_days : int64;
      }

      val make : ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      owner_address : bytes;
      name : bytes;
      abbr : bytes;
      total_supply : int64;
      frozen_supply : FrozenSupply.t list;
      trx_num : int;
          (** {%html:
<p>The fields trx_num and num define the exchange rate: num tokens can be purchased with trx_num TRX. This avoids using decimals.</p>
%}
          *)
      precision : int;
      num : int;
      start_time : int64;
      end_time : int64;
      order : int64;  (** {%html:
<p>useless</p>
%} *)
      vote_score : int;
      description : bytes;
      url : bytes;
      free_asset_net_limit : int64;
      public_free_asset_net_limit : int64;
      public_free_asset_net_usage : int64;
      public_latest_free_net_time : int64;
      id : string;
    }

    val make :
      ?owner_address:bytes ->
      ?name:bytes ->
      ?abbr:bytes ->
      ?total_supply:int64 ->
      ?frozen_supply:FrozenSupply.t list ->
      ?trx_num:int ->
      ?precision:int ->
      ?num:int ->
      ?start_time:int64 ->
      ?end_time:int64 ->
      ?order:int64 ->
      ?vote_score:int ->
      ?description:bytes ->
      ?url:bytes ->
      ?free_asset_net_limit:int64 ->
      ?public_free_asset_net_limit:int64 ->
      ?public_free_asset_net_usage:int64 ->
      ?public_latest_free_net_time:int64 ->
      ?id:string ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?name:bytes ->
      ?abbr:bytes ->
      ?total_supply:int64 ->
      ?frozen_supply:FrozenSupply.t list ->
      ?trx_num:int ->
      ?precision:int ->
      ?num:int ->
      ?start_time:int64 ->
      ?end_time:int64 ->
      ?order:int64 ->
      ?vote_score:int ->
      ?description:bytes ->
      ?url:bytes ->
      ?free_asset_net_limit:int64 ->
      ?public_free_asset_net_limit:int64 ->
      ?public_free_asset_net_usage:int64 ->
      ?public_latest_free_net_time:int64 ->
      ?id:string ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and TransferAssetContract : sig
    type t = {
      asset_name : bytes;
          (** {%html:
<p>this field is token name before the proposal ALLOW_SAME_TOKEN_NAME is active, otherwise it is token id and token is should be in string format.</p>
%}
          *)
      owner_address : bytes;
      to_address : bytes;
      amount : int64;
    }

    val make :
      ?asset_name:bytes ->
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?amount:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?asset_name:bytes ->
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?amount:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UnfreezeAssetContract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UpdateAssetContract : sig
    type t = {
      owner_address : bytes;
      description : bytes;
      url : bytes;
      new_limit : int64;
      new_public_limit : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?description:bytes ->
      ?url:bytes ->
      ?new_limit:int64 ->
      ?new_public_limit:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?description:bytes ->
      ?url:bytes ->
      ?new_limit:int64 ->
      ?new_public_limit:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and ParticipateAssetIssueContract : sig
    type t = {
      owner_address : bytes;
      to_address : bytes;
      asset_name : bytes;
          (** {%html:
<p>this field is token name before the proposal ALLOW_SAME_TOKEN_NAME is active, otherwise it is token id and token is should be in string format.</p>
%}
          *)
      amount : int64;  (** {%html:
<p>the amount of drops</p>
%} *)
    }

    val make :
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?asset_name:bytes ->
      ?amount:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?asset_name:bytes ->
      ?amount:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec AssetIssueContract : sig
    module rec FrozenSupply : sig
      type t = {
        frozen_amount : int64;  (** {%html:
<p>asset amount</p>
%} *)
        frozen_days : int64;
      }

      val make : ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      owner_address : bytes;
      name : bytes;
      abbr : bytes;
      total_supply : int64;
      frozen_supply : FrozenSupply.t list;
      trx_num : int;
          (** {%html:
<p>The fields trx_num and num define the exchange rate: num tokens can be purchased with trx_num TRX. This avoids using decimals.</p>
%}
          *)
      precision : int;
      num : int;
      start_time : int64;
      end_time : int64;
      order : int64;  (** {%html:
<p>useless</p>
%} *)
      vote_score : int;
      description : bytes;
      url : bytes;
      free_asset_net_limit : int64;
      public_free_asset_net_limit : int64;
      public_free_asset_net_usage : int64;
      public_latest_free_net_time : int64;
      id : string;
    }

    val make :
      ?owner_address:bytes ->
      ?name:bytes ->
      ?abbr:bytes ->
      ?total_supply:int64 ->
      ?frozen_supply:FrozenSupply.t list ->
      ?trx_num:int ->
      ?precision:int ->
      ?num:int ->
      ?start_time:int64 ->
      ?end_time:int64 ->
      ?order:int64 ->
      ?vote_score:int ->
      ?description:bytes ->
      ?url:bytes ->
      ?free_asset_net_limit:int64 ->
      ?public_free_asset_net_limit:int64 ->
      ?public_free_asset_net_usage:int64 ->
      ?public_latest_free_net_time:int64 ->
      ?id:string ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?name:bytes ->
      ?abbr:bytes ->
      ?total_supply:int64 ->
      ?frozen_supply:FrozenSupply.t list ->
      ?trx_num:int ->
      ?precision:int ->
      ?num:int ->
      ?start_time:int64 ->
      ?end_time:int64 ->
      ?order:int64 ->
      ?vote_score:int ->
      ?description:bytes ->
      ?url:bytes ->
      ?free_asset_net_limit:int64 ->
      ?public_free_asset_net_limit:int64 ->
      ?public_free_asset_net_usage:int64 ->
      ?public_latest_free_net_time:int64 ->
      ?id:string ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AssetIssueContract

    module rec FrozenSupply : sig
      type t = {
        frozen_amount : int64;  (** {%html:
<p>asset amount</p>
%} *)
        frozen_days : int64;
      }

      val make : ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = FrozenSupply

      let name () = ".protocol.AssetIssueContract.FrozenSupply"

      type t = { frozen_amount : int64; frozen_days : int64 }
      type make_t = ?frozen_amount:int64 -> ?frozen_days:int64 -> unit -> t

      let make ?(frozen_amount = 0L) ?(frozen_days = 0L) () =
        { frozen_amount; frozen_days }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_frozen_amount =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((1, "frozen_amount", "frozenAmount"), int64, 0L))
             in
             let merge_frozen_days =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "frozen_days", "frozenDays"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 frozen_amount =
                   merge_frozen_amount t1.frozen_amount t2.frozen_amount;
                 frozen_days = merge_frozen_days t1.frozen_days t2.frozen_days;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "frozen_amount", "frozenAmount"), int64, 0L)
          ^:: basic ((2, "frozen_days", "frozenDays"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { frozen_amount; frozen_days } ->
          serialize writer frozen_amount frozen_days

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor frozen_amount frozen_days =
          { frozen_amount; frozen_days }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { frozen_amount; frozen_days } ->
          serialize frozen_amount frozen_days

      let from_json_exn =
        let constructor frozen_amount frozen_days =
          { frozen_amount; frozen_days }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    let name () = ".protocol.AssetIssueContract"

    type t = {
      owner_address : bytes;
      name : bytes;
      abbr : bytes;
      total_supply : int64;
      frozen_supply : FrozenSupply.t list;
      trx_num : int;
      precision : int;
      num : int;
      start_time : int64;
      end_time : int64;
      order : int64;
      vote_score : int;
      description : bytes;
      url : bytes;
      free_asset_net_limit : int64;
      public_free_asset_net_limit : int64;
      public_free_asset_net_usage : int64;
      public_latest_free_net_time : int64;
      id : string;
    }

    type make_t =
      ?owner_address:bytes ->
      ?name:bytes ->
      ?abbr:bytes ->
      ?total_supply:int64 ->
      ?frozen_supply:FrozenSupply.t list ->
      ?trx_num:int ->
      ?precision:int ->
      ?num:int ->
      ?start_time:int64 ->
      ?end_time:int64 ->
      ?order:int64 ->
      ?vote_score:int ->
      ?description:bytes ->
      ?url:bytes ->
      ?free_asset_net_limit:int64 ->
      ?public_free_asset_net_limit:int64 ->
      ?public_free_asset_net_usage:int64 ->
      ?public_latest_free_net_time:int64 ->
      ?id:string ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(name = Bytes.of_string {||}) ?(abbr = Bytes.of_string {||})
        ?(total_supply = 0L) ?(frozen_supply = []) ?(trx_num = 0)
        ?(precision = 0) ?(num = 0) ?(start_time = 0L) ?(end_time = 0L)
        ?(order = 0L) ?(vote_score = 0) ?(description = Bytes.of_string {||})
        ?(url = Bytes.of_string {||}) ?(free_asset_net_limit = 0L)
        ?(public_free_asset_net_limit = 0L) ?(public_free_asset_net_usage = 0L)
        ?(public_latest_free_net_time = 0L) ?(id = {||}) () =
      {
        owner_address;
        name;
        abbr;
        total_supply;
        frozen_supply;
        trx_num;
        precision;
        num;
        start_time;
        end_time;
        order;
        vote_score;
        description;
        url;
        free_asset_net_limit;
        public_free_asset_net_limit;
        public_free_asset_net_usage;
        public_latest_free_net_time;
        id;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_name =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "name", "name"), bytes, Bytes.of_string {||}))
           in
           let merge_abbr =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "abbr", "abbr"), bytes, Bytes.of_string {||}))
           in
           let merge_total_supply =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((4, "total_supply", "totalSupply"), int64, 0L))
           in
           let merge_frozen_supply =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (5, "frozen_supply", "frozenSupply"),
                     message (module FrozenSupply),
                     not_packed ))
           in
           let merge_trx_num =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((6, "trx_num", "trxNum"), int32_int, 0))
           in
           let merge_precision =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((7, "precision", "precision"), int32_int, 0))
           in
           let merge_num =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((8, "num", "num"), int32_int, 0))
           in
           let merge_start_time =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((9, "start_time", "startTime"), int64, 0L))
           in
           let merge_end_time =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((10, "end_time", "endTime"), int64, 0L))
           in
           let merge_order =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((11, "order", "order"), int64, 0L))
           in
           let merge_vote_score =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((16, "vote_score", "voteScore"), int32_int, 0))
           in
           let merge_description =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (20, "description", "description"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_url =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((21, "url", "url"), bytes, Bytes.of_string {||}))
           in
           let merge_free_asset_net_limit =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((22, "free_asset_net_limit", "freeAssetNetLimit"), int64, 0L))
           in
           let merge_public_free_asset_net_limit =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( ( 23,
                       "public_free_asset_net_limit",
                       "publicFreeAssetNetLimit" ),
                     int64,
                     0L ))
           in
           let merge_public_free_asset_net_usage =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( ( 24,
                       "public_free_asset_net_usage",
                       "publicFreeAssetNetUsage" ),
                     int64,
                     0L ))
           in
           let merge_public_latest_free_net_time =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( ( 25,
                       "public_latest_free_net_time",
                       "publicLatestFreeNetTime" ),
                     int64,
                     0L ))
           in
           let merge_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((41, "id", "id"), string, {||}))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               name = merge_name t1.name t2.name;
               abbr = merge_abbr t1.abbr t2.abbr;
               total_supply = merge_total_supply t1.total_supply t2.total_supply;
               frozen_supply =
                 merge_frozen_supply t1.frozen_supply t2.frozen_supply;
               trx_num = merge_trx_num t1.trx_num t2.trx_num;
               precision = merge_precision t1.precision t2.precision;
               num = merge_num t1.num t2.num;
               start_time = merge_start_time t1.start_time t2.start_time;
               end_time = merge_end_time t1.end_time t2.end_time;
               order = merge_order t1.order t2.order;
               vote_score = merge_vote_score t1.vote_score t2.vote_score;
               description = merge_description t1.description t2.description;
               url = merge_url t1.url t2.url;
               free_asset_net_limit =
                 merge_free_asset_net_limit t1.free_asset_net_limit
                   t2.free_asset_net_limit;
               public_free_asset_net_limit =
                 merge_public_free_asset_net_limit
                   t1.public_free_asset_net_limit t2.public_free_asset_net_limit;
               public_free_asset_net_usage =
                 merge_public_free_asset_net_usage
                   t1.public_free_asset_net_usage t2.public_free_asset_net_usage;
               public_latest_free_net_time =
                 merge_public_latest_free_net_time
                   t1.public_latest_free_net_time t2.public_latest_free_net_time;
               id = merge_id t1.id t2.id;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "name", "name"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "abbr", "abbr"), bytes, Bytes.of_string {||})
        ^:: basic ((4, "total_supply", "totalSupply"), int64, 0L)
        ^:: repeated
              ( (5, "frozen_supply", "frozenSupply"),
                message (module FrozenSupply),
                not_packed )
        ^:: basic ((6, "trx_num", "trxNum"), int32_int, 0)
        ^:: basic ((7, "precision", "precision"), int32_int, 0)
        ^:: basic ((8, "num", "num"), int32_int, 0)
        ^:: basic ((9, "start_time", "startTime"), int64, 0L)
        ^:: basic ((10, "end_time", "endTime"), int64, 0L)
        ^:: basic ((11, "order", "order"), int64, 0L)
        ^:: basic ((16, "vote_score", "voteScore"), int32_int, 0)
        ^:: basic
              ((20, "description", "description"), bytes, Bytes.of_string {||})
        ^:: basic ((21, "url", "url"), bytes, Bytes.of_string {||})
        ^:: basic ((22, "free_asset_net_limit", "freeAssetNetLimit"), int64, 0L)
        ^:: basic
              ( (23, "public_free_asset_net_limit", "publicFreeAssetNetLimit"),
                int64,
                0L )
        ^:: basic
              ( (24, "public_free_asset_net_usage", "publicFreeAssetNetUsage"),
                int64,
                0L )
        ^:: basic
              ( (25, "public_latest_free_net_time", "publicLatestFreeNetTime"),
                int64,
                0L )
        ^:: basic ((41, "id", "id"), string, {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          owner_address;
          name;
          abbr;
          total_supply;
          frozen_supply;
          trx_num;
          precision;
          num;
          start_time;
          end_time;
          order;
          vote_score;
          description;
          url;
          free_asset_net_limit;
          public_free_asset_net_limit;
          public_free_asset_net_usage;
          public_latest_free_net_time;
          id;
        }
      ->
        serialize writer owner_address name abbr total_supply frozen_supply
          trx_num precision num start_time end_time order vote_score description
          url free_asset_net_limit public_free_asset_net_limit
          public_free_asset_net_usage public_latest_free_net_time id

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address name abbr total_supply frozen_supply trx_num
          precision num start_time end_time order vote_score description url
          free_asset_net_limit public_free_asset_net_limit
          public_free_asset_net_usage public_latest_free_net_time id =
        {
          owner_address;
          name;
          abbr;
          total_supply;
          frozen_supply;
          trx_num;
          precision;
          num;
          start_time;
          end_time;
          order;
          vote_score;
          description;
          url;
          free_asset_net_limit;
          public_free_asset_net_limit;
          public_free_asset_net_usage;
          public_latest_free_net_time;
          id;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            owner_address;
            name;
            abbr;
            total_supply;
            frozen_supply;
            trx_num;
            precision;
            num;
            start_time;
            end_time;
            order;
            vote_score;
            description;
            url;
            free_asset_net_limit;
            public_free_asset_net_limit;
            public_free_asset_net_usage;
            public_latest_free_net_time;
            id;
          }
      ->
        serialize owner_address name abbr total_supply frozen_supply trx_num
          precision num start_time end_time order vote_score description url
          free_asset_net_limit public_free_asset_net_limit
          public_free_asset_net_usage public_latest_free_net_time id

    let from_json_exn =
      let constructor owner_address name abbr total_supply frozen_supply trx_num
          precision num start_time end_time order vote_score description url
          free_asset_net_limit public_free_asset_net_limit
          public_free_asset_net_usage public_latest_free_net_time id =
        {
          owner_address;
          name;
          abbr;
          total_supply;
          frozen_supply;
          trx_num;
          precision;
          num;
          start_time;
          end_time;
          order;
          vote_score;
          description;
          url;
          free_asset_net_limit;
          public_free_asset_net_limit;
          public_free_asset_net_usage;
          public_latest_free_net_time;
          id;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and TransferAssetContract : sig
    type t = {
      asset_name : bytes;
          (** {%html:
<p>this field is token name before the proposal ALLOW_SAME_TOKEN_NAME is active, otherwise it is token id and token is should be in string format.</p>
%}
          *)
      owner_address : bytes;
      to_address : bytes;
      amount : int64;
    }

    val make :
      ?asset_name:bytes ->
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?amount:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?asset_name:bytes ->
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?amount:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = TransferAssetContract

    let name () = ".protocol.TransferAssetContract"

    type t = {
      asset_name : bytes;
      owner_address : bytes;
      to_address : bytes;
      amount : int64;
    }

    type make_t =
      ?asset_name:bytes ->
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?amount:int64 ->
      unit ->
      t

    let make ?(asset_name = Bytes.of_string {||})
        ?(owner_address = Bytes.of_string {||})
        ?(to_address = Bytes.of_string {||}) ?(amount = 0L) () =
      { asset_name; owner_address; to_address; amount }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_asset_name =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((1, "asset_name", "assetName"), bytes, Bytes.of_string {||}))
           in
           let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_to_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((3, "to_address", "toAddress"), bytes, Bytes.of_string {||}))
           in
           let merge_amount =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "amount", "amount"), int64, 0L))
           in
           fun t1 t2 ->
             {
               asset_name = merge_asset_name t1.asset_name t2.asset_name;
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               to_address = merge_to_address t1.to_address t2.to_address;
               amount = merge_amount t1.amount t2.amount;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "asset_name", "assetName"), bytes, Bytes.of_string {||})
        ^:: basic
              ((2, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "to_address", "toAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((4, "amount", "amount"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { asset_name; owner_address; to_address; amount } ->
        serialize writer asset_name owner_address to_address amount

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor asset_name owner_address to_address amount =
        { asset_name; owner_address; to_address; amount }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { asset_name; owner_address; to_address; amount } ->
        serialize asset_name owner_address to_address amount

    let from_json_exn =
      let constructor asset_name owner_address to_address amount =
        { asset_name; owner_address; to_address; amount }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UnfreezeAssetContract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UnfreezeAssetContract

    let name () = ".protocol.UnfreezeAssetContract"

    type t = bytes
    type make_t = ?owner_address:bytes -> unit -> t

    let make ?(owner_address = Bytes.of_string {||}) () = owner_address

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1_owner_address t2_owner_address ->
             merge_owner_address t1_owner_address t2_owner_address)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer owner_address -> serialize writer owner_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun owner_address -> serialize owner_address

    let from_json_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UpdateAssetContract : sig
    type t = {
      owner_address : bytes;
      description : bytes;
      url : bytes;
      new_limit : int64;
      new_public_limit : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?description:bytes ->
      ?url:bytes ->
      ?new_limit:int64 ->
      ?new_public_limit:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?description:bytes ->
      ?url:bytes ->
      ?new_limit:int64 ->
      ?new_public_limit:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UpdateAssetContract

    let name () = ".protocol.UpdateAssetContract"

    type t = {
      owner_address : bytes;
      description : bytes;
      url : bytes;
      new_limit : int64;
      new_public_limit : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?description:bytes ->
      ?url:bytes ->
      ?new_limit:int64 ->
      ?new_public_limit:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(description = Bytes.of_string {||}) ?(url = Bytes.of_string {||})
        ?(new_limit = 0L) ?(new_public_limit = 0L) () =
      { owner_address; description; url; new_limit; new_public_limit }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_description =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "description", "description"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_url =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "url", "url"), bytes, Bytes.of_string {||}))
           in
           let merge_new_limit =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "new_limit", "newLimit"), int64, 0L))
           in
           let merge_new_public_limit =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((5, "new_public_limit", "newPublicLimit"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               description = merge_description t1.description t2.description;
               url = merge_url t1.url t2.url;
               new_limit = merge_new_limit t1.new_limit t2.new_limit;
               new_public_limit =
                 merge_new_public_limit t1.new_public_limit t2.new_public_limit;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ((2, "description", "description"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "url", "url"), bytes, Bytes.of_string {||})
        ^:: basic ((4, "new_limit", "newLimit"), int64, 0L)
        ^:: basic ((5, "new_public_limit", "newPublicLimit"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        { owner_address; description; url; new_limit; new_public_limit }
      ->
        serialize writer owner_address description url new_limit
          new_public_limit

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address description url new_limit new_public_limit =
        { owner_address; description; url; new_limit; new_public_limit }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; description; url; new_limit; new_public_limit } ->
        serialize owner_address description url new_limit new_public_limit

    let from_json_exn =
      let constructor owner_address description url new_limit new_public_limit =
        { owner_address; description; url; new_limit; new_public_limit }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and ParticipateAssetIssueContract : sig
    type t = {
      owner_address : bytes;
      to_address : bytes;
      asset_name : bytes;
          (** {%html:
<p>this field is token name before the proposal ALLOW_SAME_TOKEN_NAME is active, otherwise it is token id and token is should be in string format.</p>
%}
          *)
      amount : int64;  (** {%html:
<p>the amount of drops</p>
%} *)
    }

    val make :
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?asset_name:bytes ->
      ?amount:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?asset_name:bytes ->
      ?amount:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ParticipateAssetIssueContract

    let name () = ".protocol.ParticipateAssetIssueContract"

    type t = {
      owner_address : bytes;
      to_address : bytes;
      asset_name : bytes;
      amount : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?to_address:bytes ->
      ?asset_name:bytes ->
      ?amount:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(to_address = Bytes.of_string {||})
        ?(asset_name = Bytes.of_string {||}) ?(amount = 0L) () =
      { owner_address; to_address; asset_name; amount }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_to_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((2, "to_address", "toAddress"), bytes, Bytes.of_string {||}))
           in
           let merge_asset_name =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((3, "asset_name", "assetName"), bytes, Bytes.of_string {||}))
           in
           let merge_amount =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "amount", "amount"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               to_address = merge_to_address t1.to_address t2.to_address;
               asset_name = merge_asset_name t1.asset_name t2.asset_name;
               amount = merge_amount t1.amount t2.amount;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "to_address", "toAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "asset_name", "assetName"), bytes, Bytes.of_string {||})
        ^:: basic ((4, "amount", "amount"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; to_address; asset_name; amount } ->
        serialize writer owner_address to_address asset_name amount

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address to_address asset_name amount =
        { owner_address; to_address; asset_name; amount }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; to_address; asset_name; amount } ->
        serialize owner_address to_address asset_name amount

    let from_json_exn =
      let constructor owner_address to_address asset_name amount =
        { owner_address; to_address; asset_name; amount }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
