(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/smart_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Tron = Tron
end

(**/**)

module rec Protocol : sig
  module rec SmartContract : sig
    module rec ABI : sig
      module rec Entry : sig
        module rec EntryType : sig
          type t =
            | UnknownEntryType
            | Constructor
            | Function
            | Event
            | Fallback
            | Receive
            | Error

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and StateMutabilityType : sig
          type t = UnknownMutabilityType | Pure | View | Nonpayable | Payable

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and Param : sig
          type t = {
            indexed : bool;
            name : string;
            type' : string;  (** {%html:
<p>SolidityType type = 3;</p>
%} *)
          }

          val make : ?indexed:bool -> ?name:string -> ?type':string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?indexed:bool -> ?name:string -> ?type':string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        type t = {
          anonymous : bool;
          constant : bool;
          name : string;
          inputs : Param.t list;
          outputs : Param.t list;
          type' : EntryType.t;
          payable : bool;
          stateMutability : StateMutabilityType.t;
        }

        val make :
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      type t = Entry.t list

      val make : ?entrys:Entry.t list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?entrys:Entry.t list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      origin_address : bytes;
      contract_address : bytes;
      abi : ABI.t option;
      bytecode : bytes;
      call_value : int64;
      consume_user_resource_percent : int64;
      name : string;
      origin_energy_limit : int64;
      code_hash : bytes;
      trx_hash : bytes;
      version : int;
    }

    val make :
      ?origin_address:bytes ->
      ?contract_address:bytes ->
      ?abi:ABI.t ->
      ?bytecode:bytes ->
      ?call_value:int64 ->
      ?consume_user_resource_percent:int64 ->
      ?name:string ->
      ?origin_energy_limit:int64 ->
      ?code_hash:bytes ->
      ?trx_hash:bytes ->
      ?version:int ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?origin_address:bytes ->
      ?contract_address:bytes ->
      ?abi:ABI.t ->
      ?bytecode:bytes ->
      ?call_value:int64 ->
      ?consume_user_resource_percent:int64 ->
      ?name:string ->
      ?origin_energy_limit:int64 ->
      ?code_hash:bytes ->
      ?trx_hash:bytes ->
      ?version:int ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and ContractState : sig
    type t = {
      energy_usage : int64;
      energy_factor : int64;
      update_cycle : int64;
    }

    val make :
      ?energy_usage:int64 ->
      ?energy_factor:int64 ->
      ?update_cycle:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?energy_usage:int64 ->
      ?energy_factor:int64 ->
      ?update_cycle:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and CreateSmartContract : sig
    type t = {
      owner_address : bytes;
      new_contract : SmartContract.t option;
      call_token_value : int64;
      token_id : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?new_contract:SmartContract.t ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?new_contract:SmartContract.t ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and TriggerSmartContract : sig
    type t = {
      owner_address : bytes;
      contract_address : bytes;
      call_value : int64;
      data : bytes;
      call_token_value : int64;
      token_id : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?call_value:int64 ->
      ?data:bytes ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?call_value:int64 ->
      ?data:bytes ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and ClearABIContract : sig
    type t = { owner_address : bytes; contract_address : bytes }

    val make : ?owner_address:bytes -> ?contract_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> ?contract_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UpdateSettingContract : sig
    type t = {
      owner_address : bytes;
      contract_address : bytes;
      consume_user_resource_percent : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?consume_user_resource_percent:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?consume_user_resource_percent:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UpdateEnergyLimitContract : sig
    type t = {
      owner_address : bytes;
      contract_address : bytes;
      origin_energy_limit : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?origin_energy_limit:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?origin_energy_limit:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and SmartContractDataWrapper : sig
    type t = {
      smart_contract : SmartContract.t option;
      runtimecode : bytes;
      contract_state : ContractState.t option;
    }

    val make :
      ?smart_contract:SmartContract.t ->
      ?runtimecode:bytes ->
      ?contract_state:ContractState.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?smart_contract:SmartContract.t ->
      ?runtimecode:bytes ->
      ?contract_state:ContractState.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec SmartContract : sig
    module rec ABI : sig
      module rec Entry : sig
        module rec EntryType : sig
          type t =
            | UnknownEntryType
            | Constructor
            | Function
            | Event
            | Fallback
            | Receive
            | Error

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and StateMutabilityType : sig
          type t = UnknownMutabilityType | Pure | View | Nonpayable | Payable

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and Param : sig
          type t = {
            indexed : bool;
            name : string;
            type' : string;  (** {%html:
<p>SolidityType type = 3;</p>
%} *)
          }

          val make : ?indexed:bool -> ?name:string -> ?type':string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?indexed:bool -> ?name:string -> ?type':string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        type t = {
          anonymous : bool;
          constant : bool;
          name : string;
          inputs : Param.t list;
          outputs : Param.t list;
          type' : EntryType.t;
          payable : bool;
          stateMutability : StateMutabilityType.t;
        }

        val make :
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      type t = Entry.t list

      val make : ?entrys:Entry.t list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?entrys:Entry.t list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      origin_address : bytes;
      contract_address : bytes;
      abi : ABI.t option;
      bytecode : bytes;
      call_value : int64;
      consume_user_resource_percent : int64;
      name : string;
      origin_energy_limit : int64;
      code_hash : bytes;
      trx_hash : bytes;
      version : int;
    }

    val make :
      ?origin_address:bytes ->
      ?contract_address:bytes ->
      ?abi:ABI.t ->
      ?bytecode:bytes ->
      ?call_value:int64 ->
      ?consume_user_resource_percent:int64 ->
      ?name:string ->
      ?origin_energy_limit:int64 ->
      ?code_hash:bytes ->
      ?trx_hash:bytes ->
      ?version:int ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?origin_address:bytes ->
      ?contract_address:bytes ->
      ?abi:ABI.t ->
      ?bytecode:bytes ->
      ?call_value:int64 ->
      ?consume_user_resource_percent:int64 ->
      ?name:string ->
      ?origin_energy_limit:int64 ->
      ?code_hash:bytes ->
      ?trx_hash:bytes ->
      ?version:int ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = SmartContract

    module rec ABI : sig
      module rec Entry : sig
        module rec EntryType : sig
          type t =
            | UnknownEntryType
            | Constructor
            | Function
            | Event
            | Fallback
            | Receive
            | Error

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and StateMutabilityType : sig
          type t = UnknownMutabilityType | Pure | View | Nonpayable | Payable

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and Param : sig
          type t = {
            indexed : bool;
            name : string;
            type' : string;  (** {%html:
<p>SolidityType type = 3;</p>
%} *)
          }

          val make : ?indexed:bool -> ?name:string -> ?type':string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?indexed:bool -> ?name:string -> ?type':string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        type t = {
          anonymous : bool;
          constant : bool;
          name : string;
          inputs : Param.t list;
          outputs : Param.t list;
          type' : EntryType.t;
          payable : bool;
          stateMutability : StateMutabilityType.t;
        }

        val make :
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      type t = Entry.t list

      val make : ?entrys:Entry.t list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?entrys:Entry.t list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ABI

      module rec Entry : sig
        module rec EntryType : sig
          type t =
            | UnknownEntryType
            | Constructor
            | Function
            | Event
            | Fallback
            | Receive
            | Error

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and StateMutabilityType : sig
          type t = UnknownMutabilityType | Pure | View | Nonpayable | Payable

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        and Param : sig
          type t = {
            indexed : bool;
            name : string;
            type' : string;  (** {%html:
<p>SolidityType type = 3;</p>
%} *)
          }

          val make : ?indexed:bool -> ?name:string -> ?type':string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?indexed:bool -> ?name:string -> ?type':string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        type t = {
          anonymous : bool;
          constant : bool;
          name : string;
          inputs : Param.t list;
          outputs : Param.t list;
          type' : EntryType.t;
          payable : bool;
          stateMutability : StateMutabilityType.t;
        }

        val make :
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Entry

        module rec EntryType : sig
          type t =
            | UnknownEntryType
            | Constructor
            | Function
            | Event
            | Fallback
            | Receive
            | Error

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end = struct
          module This'_ = EntryType

          type t =
            | UnknownEntryType
            | Constructor
            | Function
            | Event
            | Fallback
            | Receive
            | Error

          let name () = ".protocol.SmartContract.ABI.Entry.EntryType"

          let to_int = function
            | UnknownEntryType -> 0
            | Constructor -> 1
            | Function -> 2
            | Event -> 3
            | Fallback -> 4
            | Receive -> 5
            | Error -> 6

          let from_int_exn = function
            | 0 -> UnknownEntryType
            | 1 -> Constructor
            | 2 -> Function
            | 3 -> Event
            | 4 -> Fallback
            | 5 -> Receive
            | 6 -> Error
            | n -> Runtime'.Result.raise (`Unknown_enum_value n)

          let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

          let to_string = function
            | UnknownEntryType -> "UnknownEntryType"
            | Constructor -> "Constructor"
            | Function -> "Function"
            | Event -> "Event"
            | Fallback -> "Fallback"
            | Receive -> "Receive"
            | Error -> "Error"

          let from_string_exn = function
            | "UnknownEntryType" -> UnknownEntryType
            | "Constructor" -> Constructor
            | "Function" -> Function
            | "Event" -> Event
            | "Fallback" -> Fallback
            | "Receive" -> Receive
            | "Error" -> Error
            | s -> Runtime'.Result.raise (`Unknown_enum_name s)
        end

        and StateMutabilityType : sig
          type t = UnknownMutabilityType | Pure | View | Nonpayable | Payable

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end = struct
          module This'_ = StateMutabilityType

          type t = UnknownMutabilityType | Pure | View | Nonpayable | Payable

          let name () = ".protocol.SmartContract.ABI.Entry.StateMutabilityType"

          let to_int = function
            | UnknownMutabilityType -> 0
            | Pure -> 1
            | View -> 2
            | Nonpayable -> 3
            | Payable -> 4

          let from_int_exn = function
            | 0 -> UnknownMutabilityType
            | 1 -> Pure
            | 2 -> View
            | 3 -> Nonpayable
            | 4 -> Payable
            | n -> Runtime'.Result.raise (`Unknown_enum_value n)

          let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

          let to_string = function
            | UnknownMutabilityType -> "UnknownMutabilityType"
            | Pure -> "Pure"
            | View -> "View"
            | Nonpayable -> "Nonpayable"
            | Payable -> "Payable"

          let from_string_exn = function
            | "UnknownMutabilityType" -> UnknownMutabilityType
            | "Pure" -> Pure
            | "View" -> View
            | "Nonpayable" -> Nonpayable
            | "Payable" -> Payable
            | s -> Runtime'.Result.raise (`Unknown_enum_name s)
        end

        and Param : sig
          type t = {
            indexed : bool;
            name : string;
            type' : string;  (** {%html:
<p>SolidityType type = 3;</p>
%} *)
          }

          val make : ?indexed:bool -> ?name:string -> ?type':string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?indexed:bool -> ?name:string -> ?type':string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = Param

          let name () = ".protocol.SmartContract.ABI.Entry.Param"

          type t = { indexed : bool; name : string; type' : string }

          type make_t =
            ?indexed:bool -> ?name:string -> ?type':string -> unit -> t

          let make ?(indexed = false) ?(name = {||}) ?(type' = {||}) () =
            { indexed; name; type' }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_indexed =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "indexed", "indexed"), bool, false))
                 in
                 let merge_name =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "name", "name"), string, {||}))
                 in
                 let merge_type' =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((3, "type", "type"), string, {||}))
                 in
                 fun t1 t2 ->
                   {
                     indexed = merge_indexed t1.indexed t2.indexed;
                     name = merge_name t1.name t2.name;
                     type' = merge_type' t1.type' t2.type';
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "indexed", "indexed"), bool, false)
              ^:: basic ((2, "name", "name"), string, {||})
              ^:: basic ((3, "type", "type"), string, {||})
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { indexed; name; type' } ->
              serialize writer indexed name type'

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor indexed name type' = { indexed; name; type' } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { indexed; name; type' } -> serialize indexed name type'

          let from_json_exn =
            let constructor indexed name type' = { indexed; name; type' } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        let name () = ".protocol.SmartContract.ABI.Entry"

        type t = {
          anonymous : bool;
          constant : bool;
          name : string;
          inputs : Param.t list;
          outputs : Param.t list;
          type' : EntryType.t;
          payable : bool;
          stateMutability : StateMutabilityType.t;
        }

        type make_t =
          ?anonymous:bool ->
          ?constant:bool ->
          ?name:string ->
          ?inputs:Param.t list ->
          ?outputs:Param.t list ->
          ?type':EntryType.t ->
          ?payable:bool ->
          ?stateMutability:StateMutabilityType.t ->
          unit ->
          t

        let make ?(anonymous = false) ?(constant = false) ?(name = {||})
            ?(inputs = []) ?(outputs = []) ?(type' = EntryType.from_int_exn 0)
            ?(payable = false)
            ?(stateMutability = StateMutabilityType.from_int_exn 0) () =
          {
            anonymous;
            constant;
            name;
            inputs;
            outputs;
            type';
            payable;
            stateMutability;
          }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_anonymous =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "anonymous", "anonymous"), bool, false))
               in
               let merge_constant =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "constant", "constant"), bool, false))
               in
               let merge_name =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((3, "name", "name"), string, {||}))
               in
               let merge_inputs =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (4, "inputs", "inputs"),
                         message (module Param),
                         not_packed ))
               in
               let merge_outputs =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (5, "outputs", "outputs"),
                         message (module Param),
                         not_packed ))
               in
               let merge_type' =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (6, "type", "type"),
                         enum (module EntryType),
                         EntryType.from_int_exn 0 ))
               in
               let merge_payable =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((7, "payable", "payable"), bool, false))
               in
               let merge_stateMutability =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (8, "stateMutability", "stateMutability"),
                         enum (module StateMutabilityType),
                         StateMutabilityType.from_int_exn 0 ))
               in
               fun t1 t2 ->
                 {
                   anonymous = merge_anonymous t1.anonymous t2.anonymous;
                   constant = merge_constant t1.constant t2.constant;
                   name = merge_name t1.name t2.name;
                   inputs = merge_inputs t1.inputs t2.inputs;
                   outputs = merge_outputs t1.outputs t2.outputs;
                   type' = merge_type' t1.type' t2.type';
                   payable = merge_payable t1.payable t2.payable;
                   stateMutability =
                     merge_stateMutability t1.stateMutability t2.stateMutability;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "anonymous", "anonymous"), bool, false)
            ^:: basic ((2, "constant", "constant"), bool, false)
            ^:: basic ((3, "name", "name"), string, {||})
            ^:: repeated
                  ((4, "inputs", "inputs"), message (module Param), not_packed)
            ^:: repeated
                  ((5, "outputs", "outputs"), message (module Param), not_packed)
            ^:: basic
                  ( (6, "type", "type"),
                    enum (module EntryType),
                    EntryType.from_int_exn 0 )
            ^:: basic ((7, "payable", "payable"), bool, false)
            ^:: basic
                  ( (8, "stateMutability", "stateMutability"),
                    enum (module StateMutabilityType),
                    StateMutabilityType.from_int_exn 0 )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            {
              anonymous;
              constant;
              name;
              inputs;
              outputs;
              type';
              payable;
              stateMutability;
            }
          ->
            serialize writer anonymous constant name inputs outputs type'
              payable stateMutability

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor anonymous constant name inputs outputs type' payable
              stateMutability =
            {
              anonymous;
              constant;
              name;
              inputs;
              outputs;
              type';
              payable;
              stateMutability;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun {
                anonymous;
                constant;
                name;
                inputs;
                outputs;
                type';
                payable;
                stateMutability;
              }
          ->
            serialize anonymous constant name inputs outputs type' payable
              stateMutability

        let from_json_exn =
          let constructor anonymous constant name inputs outputs type' payable
              stateMutability =
            {
              anonymous;
              constant;
              name;
              inputs;
              outputs;
              type';
              payable;
              stateMutability;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      let name () = ".protocol.SmartContract.ABI"

      type t = Entry.t list
      type make_t = ?entrys:Entry.t list -> unit -> t

      let make ?(entrys = []) () = entrys

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_entrys =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated
                     ( (1, "entrys", "entrys"),
                       message (module Entry),
                       not_packed ))
             in
             fun t1_entrys t2_entrys -> merge_entrys t1_entrys t2_entrys)
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          repeated ((1, "entrys", "entrys"), message (module Entry), not_packed)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer entrys -> serialize writer entrys

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor entrys = entrys in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun entrys -> serialize entrys

      let from_json_exn =
        let constructor entrys = entrys in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    let name () = ".protocol.SmartContract"

    type t = {
      origin_address : bytes;
      contract_address : bytes;
      abi : ABI.t option;
      bytecode : bytes;
      call_value : int64;
      consume_user_resource_percent : int64;
      name : string;
      origin_energy_limit : int64;
      code_hash : bytes;
      trx_hash : bytes;
      version : int;
    }

    type make_t =
      ?origin_address:bytes ->
      ?contract_address:bytes ->
      ?abi:ABI.t ->
      ?bytecode:bytes ->
      ?call_value:int64 ->
      ?consume_user_resource_percent:int64 ->
      ?name:string ->
      ?origin_energy_limit:int64 ->
      ?code_hash:bytes ->
      ?trx_hash:bytes ->
      ?version:int ->
      unit ->
      t

    let make ?(origin_address = Bytes.of_string {||})
        ?(contract_address = Bytes.of_string {||}) ?abi
        ?(bytecode = Bytes.of_string {||}) ?(call_value = 0L)
        ?(consume_user_resource_percent = 0L) ?(name = {||})
        ?(origin_energy_limit = 0L) ?(code_hash = Bytes.of_string {||})
        ?(trx_hash = Bytes.of_string {||}) ?(version = 0) () =
      {
        origin_address;
        contract_address;
        abi;
        bytecode;
        call_value;
        consume_user_resource_percent;
        name;
        origin_energy_limit;
        code_hash;
        trx_hash;
        version;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_origin_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "origin_address", "originAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_contract_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "contract_address", "contractAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_abi =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((3, "abi", "abi"), message (module ABI)))
           in
           let merge_bytecode =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((4, "bytecode", "bytecode"), bytes, Bytes.of_string {||}))
           in
           let merge_call_value =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((5, "call_value", "callValue"), int64, 0L))
           in
           let merge_consume_user_resource_percent =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( ( 6,
                       "consume_user_resource_percent",
                       "consumeUserResourcePercent" ),
                     int64,
                     0L ))
           in
           let merge_name =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((7, "name", "name"), string, {||}))
           in
           let merge_origin_energy_limit =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((8, "origin_energy_limit", "originEnergyLimit"), int64, 0L))
           in
           let merge_code_hash =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((9, "code_hash", "codeHash"), bytes, Bytes.of_string {||}))
           in
           let merge_trx_hash =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((10, "trx_hash", "trxHash"), bytes, Bytes.of_string {||}))
           in
           let merge_version =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((11, "version", "version"), int32_int, 0))
           in
           fun t1 t2 ->
             {
               origin_address =
                 merge_origin_address t1.origin_address t2.origin_address;
               contract_address =
                 merge_contract_address t1.contract_address t2.contract_address;
               abi = merge_abi t1.abi t2.abi;
               bytecode = merge_bytecode t1.bytecode t2.bytecode;
               call_value = merge_call_value t1.call_value t2.call_value;
               consume_user_resource_percent =
                 merge_consume_user_resource_percent
                   t1.consume_user_resource_percent
                   t2.consume_user_resource_percent;
               name = merge_name t1.name t2.name;
               origin_energy_limit =
                 merge_origin_energy_limit t1.origin_energy_limit
                   t2.origin_energy_limit;
               code_hash = merge_code_hash t1.code_hash t2.code_hash;
               trx_hash = merge_trx_hash t1.trx_hash t2.trx_hash;
               version = merge_version t1.version t2.version;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic
          ((1, "origin_address", "originAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "contract_address", "contractAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic_opt ((3, "abi", "abi"), message (module ABI))
        ^:: basic ((4, "bytecode", "bytecode"), bytes, Bytes.of_string {||})
        ^:: basic ((5, "call_value", "callValue"), int64, 0L)
        ^:: basic
              ( ( 6,
                  "consume_user_resource_percent",
                  "consumeUserResourcePercent" ),
                int64,
                0L )
        ^:: basic ((7, "name", "name"), string, {||})
        ^:: basic ((8, "origin_energy_limit", "originEnergyLimit"), int64, 0L)
        ^:: basic ((9, "code_hash", "codeHash"), bytes, Bytes.of_string {||})
        ^:: basic ((10, "trx_hash", "trxHash"), bytes, Bytes.of_string {||})
        ^:: basic ((11, "version", "version"), int32_int, 0)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          origin_address;
          contract_address;
          abi;
          bytecode;
          call_value;
          consume_user_resource_percent;
          name;
          origin_energy_limit;
          code_hash;
          trx_hash;
          version;
        }
      ->
        serialize writer origin_address contract_address abi bytecode call_value
          consume_user_resource_percent name origin_energy_limit code_hash
          trx_hash version

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor origin_address contract_address abi bytecode call_value
          consume_user_resource_percent name origin_energy_limit code_hash
          trx_hash version =
        {
          origin_address;
          contract_address;
          abi;
          bytecode;
          call_value;
          consume_user_resource_percent;
          name;
          origin_energy_limit;
          code_hash;
          trx_hash;
          version;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            origin_address;
            contract_address;
            abi;
            bytecode;
            call_value;
            consume_user_resource_percent;
            name;
            origin_energy_limit;
            code_hash;
            trx_hash;
            version;
          }
      ->
        serialize origin_address contract_address abi bytecode call_value
          consume_user_resource_percent name origin_energy_limit code_hash
          trx_hash version

    let from_json_exn =
      let constructor origin_address contract_address abi bytecode call_value
          consume_user_resource_percent name origin_energy_limit code_hash
          trx_hash version =
        {
          origin_address;
          contract_address;
          abi;
          bytecode;
          call_value;
          consume_user_resource_percent;
          name;
          origin_energy_limit;
          code_hash;
          trx_hash;
          version;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and ContractState : sig
    type t = {
      energy_usage : int64;
      energy_factor : int64;
      update_cycle : int64;
    }

    val make :
      ?energy_usage:int64 ->
      ?energy_factor:int64 ->
      ?update_cycle:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?energy_usage:int64 ->
      ?energy_factor:int64 ->
      ?update_cycle:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ContractState

    let name () = ".protocol.ContractState"

    type t = {
      energy_usage : int64;
      energy_factor : int64;
      update_cycle : int64;
    }

    type make_t =
      ?energy_usage:int64 ->
      ?energy_factor:int64 ->
      ?update_cycle:int64 ->
      unit ->
      t

    let make ?(energy_usage = 0L) ?(energy_factor = 0L) ?(update_cycle = 0L) ()
        =
      { energy_usage; energy_factor; update_cycle }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_energy_usage =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((1, "energy_usage", "energyUsage"), int64, 0L))
           in
           let merge_energy_factor =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "energy_factor", "energyFactor"), int64, 0L))
           in
           let merge_update_cycle =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "update_cycle", "updateCycle"), int64, 0L))
           in
           fun t1 t2 ->
             {
               energy_usage = merge_energy_usage t1.energy_usage t2.energy_usage;
               energy_factor =
                 merge_energy_factor t1.energy_factor t2.energy_factor;
               update_cycle = merge_update_cycle t1.update_cycle t2.update_cycle;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "energy_usage", "energyUsage"), int64, 0L)
        ^:: basic ((2, "energy_factor", "energyFactor"), int64, 0L)
        ^:: basic ((3, "update_cycle", "updateCycle"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { energy_usage; energy_factor; update_cycle } ->
        serialize writer energy_usage energy_factor update_cycle

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor energy_usage energy_factor update_cycle =
        { energy_usage; energy_factor; update_cycle }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { energy_usage; energy_factor; update_cycle } ->
        serialize energy_usage energy_factor update_cycle

    let from_json_exn =
      let constructor energy_usage energy_factor update_cycle =
        { energy_usage; energy_factor; update_cycle }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and CreateSmartContract : sig
    type t = {
      owner_address : bytes;
      new_contract : SmartContract.t option;
      call_token_value : int64;
      token_id : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?new_contract:SmartContract.t ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?new_contract:SmartContract.t ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = CreateSmartContract

    let name () = ".protocol.CreateSmartContract"

    type t = {
      owner_address : bytes;
      new_contract : SmartContract.t option;
      call_token_value : int64;
      token_id : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?new_contract:SmartContract.t ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||}) ?new_contract
        ?(call_token_value = 0L) ?(token_id = 0L) () =
      { owner_address; new_contract; call_token_value; token_id }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_new_contract =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (2, "new_contract", "newContract"),
                     message (module SmartContract) ))
           in
           let merge_call_token_value =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "call_token_value", "callTokenValue"), int64, 0L))
           in
           let merge_token_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "token_id", "tokenId"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               new_contract = merge_new_contract t1.new_contract t2.new_contract;
               call_token_value =
                 merge_call_token_value t1.call_token_value t2.call_token_value;
               token_id = merge_token_id t1.token_id t2.token_id;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic_opt
              ( (2, "new_contract", "newContract"),
                message (module SmartContract) )
        ^:: basic ((3, "call_token_value", "callTokenValue"), int64, 0L)
        ^:: basic ((4, "token_id", "tokenId"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; new_contract; call_token_value; token_id } ->
        serialize writer owner_address new_contract call_token_value token_id

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address new_contract call_token_value token_id =
        { owner_address; new_contract; call_token_value; token_id }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; new_contract; call_token_value; token_id } ->
        serialize owner_address new_contract call_token_value token_id

    let from_json_exn =
      let constructor owner_address new_contract call_token_value token_id =
        { owner_address; new_contract; call_token_value; token_id }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and TriggerSmartContract : sig
    type t = {
      owner_address : bytes;
      contract_address : bytes;
      call_value : int64;
      data : bytes;
      call_token_value : int64;
      token_id : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?call_value:int64 ->
      ?data:bytes ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?call_value:int64 ->
      ?data:bytes ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = TriggerSmartContract

    let name () = ".protocol.TriggerSmartContract"

    type t = {
      owner_address : bytes;
      contract_address : bytes;
      call_value : int64;
      data : bytes;
      call_token_value : int64;
      token_id : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?call_value:int64 ->
      ?data:bytes ->
      ?call_token_value:int64 ->
      ?token_id:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(contract_address = Bytes.of_string {||}) ?(call_value = 0L)
        ?(data = Bytes.of_string {||}) ?(call_token_value = 0L) ?(token_id = 0L)
        () =
      {
        owner_address;
        contract_address;
        call_value;
        data;
        call_token_value;
        token_id;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_contract_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "contract_address", "contractAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_call_value =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "call_value", "callValue"), int64, 0L))
           in
           let merge_data =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((4, "data", "data"), bytes, Bytes.of_string {||}))
           in
           let merge_call_token_value =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((5, "call_token_value", "callTokenValue"), int64, 0L))
           in
           let merge_token_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((6, "token_id", "tokenId"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               contract_address =
                 merge_contract_address t1.contract_address t2.contract_address;
               call_value = merge_call_value t1.call_value t2.call_value;
               data = merge_data t1.data t2.data;
               call_token_value =
                 merge_call_token_value t1.call_token_value t2.call_token_value;
               token_id = merge_token_id t1.token_id t2.token_id;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "contract_address", "contractAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic ((3, "call_value", "callValue"), int64, 0L)
        ^:: basic ((4, "data", "data"), bytes, Bytes.of_string {||})
        ^:: basic ((5, "call_token_value", "callTokenValue"), int64, 0L)
        ^:: basic ((6, "token_id", "tokenId"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          owner_address;
          contract_address;
          call_value;
          data;
          call_token_value;
          token_id;
        }
      ->
        serialize writer owner_address contract_address call_value data
          call_token_value token_id

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address contract_address call_value data
          call_token_value token_id =
        {
          owner_address;
          contract_address;
          call_value;
          data;
          call_token_value;
          token_id;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            owner_address;
            contract_address;
            call_value;
            data;
            call_token_value;
            token_id;
          }
      ->
        serialize owner_address contract_address call_value data
          call_token_value token_id

    let from_json_exn =
      let constructor owner_address contract_address call_value data
          call_token_value token_id =
        {
          owner_address;
          contract_address;
          call_value;
          data;
          call_token_value;
          token_id;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and ClearABIContract : sig
    type t = { owner_address : bytes; contract_address : bytes }

    val make : ?owner_address:bytes -> ?contract_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> ?contract_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ClearABIContract

    let name () = ".protocol.ClearABIContract"

    type t = { owner_address : bytes; contract_address : bytes }
    type make_t = ?owner_address:bytes -> ?contract_address:bytes -> unit -> t

    let make ?(owner_address = Bytes.of_string {||})
        ?(contract_address = Bytes.of_string {||}) () =
      { owner_address; contract_address }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_contract_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "contract_address", "contractAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               contract_address =
                 merge_contract_address t1.contract_address t2.contract_address;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "contract_address", "contractAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; contract_address } ->
        serialize writer owner_address contract_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address contract_address =
        { owner_address; contract_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; contract_address } ->
        serialize owner_address contract_address

    let from_json_exn =
      let constructor owner_address contract_address =
        { owner_address; contract_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UpdateSettingContract : sig
    type t = {
      owner_address : bytes;
      contract_address : bytes;
      consume_user_resource_percent : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?consume_user_resource_percent:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?consume_user_resource_percent:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UpdateSettingContract

    let name () = ".protocol.UpdateSettingContract"

    type t = {
      owner_address : bytes;
      contract_address : bytes;
      consume_user_resource_percent : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?consume_user_resource_percent:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(contract_address = Bytes.of_string {||})
        ?(consume_user_resource_percent = 0L) () =
      { owner_address; contract_address; consume_user_resource_percent }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_contract_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "contract_address", "contractAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_consume_user_resource_percent =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( ( 3,
                       "consume_user_resource_percent",
                       "consumeUserResourcePercent" ),
                     int64,
                     0L ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               contract_address =
                 merge_contract_address t1.contract_address t2.contract_address;
               consume_user_resource_percent =
                 merge_consume_user_resource_percent
                   t1.consume_user_resource_percent
                   t2.consume_user_resource_percent;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "contract_address", "contractAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic
              ( ( 3,
                  "consume_user_resource_percent",
                  "consumeUserResourcePercent" ),
                int64,
                0L )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        { owner_address; contract_address; consume_user_resource_percent }
      ->
        serialize writer owner_address contract_address
          consume_user_resource_percent

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address contract_address
          consume_user_resource_percent =
        { owner_address; contract_address; consume_user_resource_percent }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; contract_address; consume_user_resource_percent } ->
        serialize owner_address contract_address consume_user_resource_percent

    let from_json_exn =
      let constructor owner_address contract_address
          consume_user_resource_percent =
        { owner_address; contract_address; consume_user_resource_percent }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UpdateEnergyLimitContract : sig
    type t = {
      owner_address : bytes;
      contract_address : bytes;
      origin_energy_limit : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?origin_energy_limit:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?origin_energy_limit:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UpdateEnergyLimitContract

    let name () = ".protocol.UpdateEnergyLimitContract"

    type t = {
      owner_address : bytes;
      contract_address : bytes;
      origin_energy_limit : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?contract_address:bytes ->
      ?origin_energy_limit:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(contract_address = Bytes.of_string {||}) ?(origin_energy_limit = 0L)
        () =
      { owner_address; contract_address; origin_energy_limit }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_contract_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "contract_address", "contractAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_origin_energy_limit =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((3, "origin_energy_limit", "originEnergyLimit"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               contract_address =
                 merge_contract_address t1.contract_address t2.contract_address;
               origin_energy_limit =
                 merge_origin_energy_limit t1.origin_energy_limit
                   t2.origin_energy_limit;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "contract_address", "contractAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic ((3, "origin_energy_limit", "originEnergyLimit"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; contract_address; origin_energy_limit } ->
        serialize writer owner_address contract_address origin_energy_limit

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address contract_address origin_energy_limit =
        { owner_address; contract_address; origin_energy_limit }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; contract_address; origin_energy_limit } ->
        serialize owner_address contract_address origin_energy_limit

    let from_json_exn =
      let constructor owner_address contract_address origin_energy_limit =
        { owner_address; contract_address; origin_energy_limit }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SmartContractDataWrapper : sig
    type t = {
      smart_contract : SmartContract.t option;
      runtimecode : bytes;
      contract_state : ContractState.t option;
    }

    val make :
      ?smart_contract:SmartContract.t ->
      ?runtimecode:bytes ->
      ?contract_state:ContractState.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?smart_contract:SmartContract.t ->
      ?runtimecode:bytes ->
      ?contract_state:ContractState.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = SmartContractDataWrapper

    let name () = ".protocol.SmartContractDataWrapper"

    type t = {
      smart_contract : SmartContract.t option;
      runtimecode : bytes;
      contract_state : ContractState.t option;
    }

    type make_t =
      ?smart_contract:SmartContract.t ->
      ?runtimecode:bytes ->
      ?contract_state:ContractState.t ->
      unit ->
      t

    let make ?smart_contract ?(runtimecode = Bytes.of_string {||})
        ?contract_state () =
      { smart_contract; runtimecode; contract_state }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_smart_contract =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (1, "smart_contract", "smartContract"),
                     message (module SmartContract) ))
           in
           let merge_runtimecode =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "runtimecode", "runtimecode"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_contract_state =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (3, "contract_state", "contractState"),
                     message (module ContractState) ))
           in
           fun t1 t2 ->
             {
               smart_contract =
                 merge_smart_contract t1.smart_contract t2.smart_contract;
               runtimecode = merge_runtimecode t1.runtimecode t2.runtimecode;
               contract_state =
                 merge_contract_state t1.contract_state t2.contract_state;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt
          ( (1, "smart_contract", "smartContract"),
            message (module SmartContract) )
        ^:: basic
              ((2, "runtimecode", "runtimecode"), bytes, Bytes.of_string {||})
        ^:: basic_opt
              ( (3, "contract_state", "contractState"),
                message (module ContractState) )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { smart_contract; runtimecode; contract_state } ->
        serialize writer smart_contract runtimecode contract_state

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor smart_contract runtimecode contract_state =
        { smart_contract; runtimecode; contract_state }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { smart_contract; runtimecode; contract_state } ->
        serialize smart_contract runtimecode contract_state

    let from_json_exn =
      let constructor smart_contract runtimecode contract_state =
        { smart_contract; runtimecode; contract_state }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
