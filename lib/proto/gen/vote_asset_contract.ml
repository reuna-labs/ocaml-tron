(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/vote_asset_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct end

(**/**)

module rec Protocol : sig
  module rec VoteAssetContract : sig
    type t = {
      owner_address : bytes;
      vote_address : bytes list;
      support : bool;
      count : int;
    }

    val make :
      ?owner_address:bytes ->
      ?vote_address:bytes list ->
      ?support:bool ->
      ?count:int ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?vote_address:bytes list ->
      ?support:bool ->
      ?count:int ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec VoteAssetContract : sig
    type t = {
      owner_address : bytes;
      vote_address : bytes list;
      support : bool;
      count : int;
    }

    val make :
      ?owner_address:bytes ->
      ?vote_address:bytes list ->
      ?support:bool ->
      ?count:int ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?vote_address:bytes list ->
      ?support:bool ->
      ?count:int ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = VoteAssetContract

    let name () = ".protocol.VoteAssetContract"

    type t = {
      owner_address : bytes;
      vote_address : bytes list;
      support : bool;
      count : int;
    }

    type make_t =
      ?owner_address:bytes ->
      ?vote_address:bytes list ->
      ?support:bool ->
      ?count:int ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||}) ?(vote_address = [])
        ?(support = false) ?(count = 0) () =
      { owner_address; vote_address; support; count }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_vote_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated ((2, "vote_address", "voteAddress"), bytes, not_packed))
           in
           let merge_support =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "support", "support"), bool, false))
           in
           let merge_count =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((5, "count", "count"), int32_int, 0))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               vote_address = merge_vote_address t1.vote_address t2.vote_address;
               support = merge_support t1.support t2.support;
               count = merge_count t1.count t2.count;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: repeated ((2, "vote_address", "voteAddress"), bytes, not_packed)
        ^:: basic ((3, "support", "support"), bool, false)
        ^:: basic ((5, "count", "count"), int32_int, 0)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; vote_address; support; count } ->
        serialize writer owner_address vote_address support count

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address vote_address support count =
        { owner_address; vote_address; support; count }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; vote_address; support; count } ->
        serialize owner_address vote_address support count

    let from_json_exn =
      let constructor owner_address vote_address support count =
        { owner_address; vote_address; support; count }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
