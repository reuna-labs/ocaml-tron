(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/shield_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct end

(**/**)

module rec Protocol : sig
  (** {%html:
<p>for shielded transaction</p>
%} *)
  module rec AuthenticationPath : sig
    type t = bool list

    val make : ?value:bool list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?value:bool list -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and MerklePath : sig
    type t = {
      authentication_paths : AuthenticationPath.t list;
      index : bool list;
      rt : bytes;
    }

    val make :
      ?authentication_paths:AuthenticationPath.t list ->
      ?index:bool list ->
      ?rt:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?authentication_paths:AuthenticationPath.t list ->
      ?index:bool list ->
      ?rt:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and OutputPoint : sig
    type t = { hash : bytes; index : int }

    val make : ?hash:bytes -> ?index:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?hash:bytes -> ?index:int -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and OutputPointInfo : sig
    type t = { out_points : OutputPoint.t list; block_num : int }

    val make : ?out_points:OutputPoint.t list -> ?block_num:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?out_points:OutputPoint.t list -> ?block_num:int -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and PedersenHash : sig
    type t = bytes

    val make : ?content:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?content:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and IncrementalMerkleTree : sig
    type t = {
      left : PedersenHash.t option;
      right : PedersenHash.t option;
      parents : PedersenHash.t list;
    }

    val make :
      ?left:PedersenHash.t ->
      ?right:PedersenHash.t ->
      ?parents:PedersenHash.t list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?left:PedersenHash.t ->
      ?right:PedersenHash.t ->
      ?parents:PedersenHash.t list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and IncrementalMerkleVoucher : sig
    type t = {
      tree : IncrementalMerkleTree.t option;
      filled : PedersenHash.t list;
      cursor : IncrementalMerkleTree.t option;
      cursor_depth : int64;
      rt : bytes;
      output_point : OutputPoint.t option;
    }

    val make :
      ?tree:IncrementalMerkleTree.t ->
      ?filled:PedersenHash.t list ->
      ?cursor:IncrementalMerkleTree.t ->
      ?cursor_depth:int64 ->
      ?rt:bytes ->
      ?output_point:OutputPoint.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?tree:IncrementalMerkleTree.t ->
      ?filled:PedersenHash.t list ->
      ?cursor:IncrementalMerkleTree.t ->
      ?cursor_depth:int64 ->
      ?rt:bytes ->
      ?output_point:OutputPoint.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and IncrementalMerkleVoucherInfo : sig
    type t = { vouchers : IncrementalMerkleVoucher.t list; paths : bytes list }

    val make :
      ?vouchers:IncrementalMerkleVoucher.t list ->
      ?paths:bytes list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?vouchers:IncrementalMerkleVoucher.t list ->
      ?paths:bytes list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and SpendDescription : sig
    type t = {
      value_commitment : bytes;
      anchor : bytes;  (** {%html:
<p>merkle root</p>
%} *)
      nullifier : bytes;  (** {%html:
<p>used for check double spend</p>
%} *)
      rk : bytes;
          (** {%html:
<p>used for check spend authority signature</p>
%} *)
      zkproof : bytes;
      spend_authority_signature : bytes;
    }

    val make :
      ?value_commitment:bytes ->
      ?anchor:bytes ->
      ?nullifier:bytes ->
      ?rk:bytes ->
      ?zkproof:bytes ->
      ?spend_authority_signature:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?value_commitment:bytes ->
      ?anchor:bytes ->
      ?nullifier:bytes ->
      ?rk:bytes ->
      ?zkproof:bytes ->
      ?spend_authority_signature:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and ReceiveDescription : sig
    type t = {
      value_commitment : bytes;
      note_commitment : bytes;
      epk : bytes;  (** {%html:
<p>for Encryption</p>
%} *)
      c_enc : bytes;
          (** {%html:
<p>Encryption for incoming, decrypt it with ivk</p>
%} *)
      c_out : bytes;
          (** {%html:
<p>Encryption for audit, decrypt it with ovk</p>
%} *)
      zkproof : bytes;
    }

    val make :
      ?value_commitment:bytes ->
      ?note_commitment:bytes ->
      ?epk:bytes ->
      ?c_enc:bytes ->
      ?c_out:bytes ->
      ?zkproof:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?value_commitment:bytes ->
      ?note_commitment:bytes ->
      ?epk:bytes ->
      ?c_enc:bytes ->
      ?c_out:bytes ->
      ?zkproof:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and ShieldedTransferContract : sig
    type t = {
      transparent_from_address : bytes;
          (** {%html:
<p>transparent address</p>
%} *)
      from_amount : int64;
      spend_description : SpendDescription.t list;
      receive_description : ReceiveDescription.t list;
      binding_signature : bytes;
      transparent_to_address : bytes;
          (** {%html:
<p>transparent address</p>
%} *)
      to_amount : int64;
          (** {%html:
<p>the amount to transparent to_address</p>
%} *)
    }

    val make :
      ?transparent_from_address:bytes ->
      ?from_amount:int64 ->
      ?spend_description:SpendDescription.t list ->
      ?receive_description:ReceiveDescription.t list ->
      ?binding_signature:bytes ->
      ?transparent_to_address:bytes ->
      ?to_amount:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?transparent_from_address:bytes ->
      ?from_amount:int64 ->
      ?spend_description:SpendDescription.t list ->
      ?receive_description:ReceiveDescription.t list ->
      ?binding_signature:bytes ->
      ?transparent_to_address:bytes ->
      ?to_amount:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec AuthenticationPath : sig
    type t = bool list

    val make : ?value:bool list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?value:bool list -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AuthenticationPath

    let name () = ".protocol.AuthenticationPath"

    type t = bool list
    type make_t = ?value:bool list -> unit -> t

    let make ?(value = []) () = value

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_value =
             Runtime'.Merge.merge
               Runtime'.Spec.(repeated ((1, "value", "value"), bool, packed))
           in
           fun t1_value t2_value -> merge_value t1_value t2_value)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(repeated ((1, "value", "value"), bool, packed) ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer value -> serialize writer value

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor value = value in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun value -> serialize value

    let from_json_exn =
      let constructor value = value in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and MerklePath : sig
    type t = {
      authentication_paths : AuthenticationPath.t list;
      index : bool list;
      rt : bytes;
    }

    val make :
      ?authentication_paths:AuthenticationPath.t list ->
      ?index:bool list ->
      ?rt:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?authentication_paths:AuthenticationPath.t list ->
      ?index:bool list ->
      ?rt:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = MerklePath

    let name () = ".protocol.MerklePath"

    type t = {
      authentication_paths : AuthenticationPath.t list;
      index : bool list;
      rt : bytes;
    }

    type make_t =
      ?authentication_paths:AuthenticationPath.t list ->
      ?index:bool list ->
      ?rt:bytes ->
      unit ->
      t

    let make ?(authentication_paths = []) ?(index = [])
        ?(rt = Bytes.of_string {||}) () =
      { authentication_paths; index; rt }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_authentication_paths =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (1, "authentication_paths", "authenticationPaths"),
                     message (module AuthenticationPath),
                     not_packed ))
           in
           let merge_index =
             Runtime'.Merge.merge
               Runtime'.Spec.(repeated ((2, "index", "index"), bool, packed))
           in
           let merge_rt =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "rt", "rt"), bytes, Bytes.of_string {||}))
           in
           fun t1 t2 ->
             {
               authentication_paths =
                 merge_authentication_paths t1.authentication_paths
                   t2.authentication_paths;
               index = merge_index t1.index t2.index;
               rt = merge_rt t1.rt t2.rt;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        repeated
          ( (1, "authentication_paths", "authenticationPaths"),
            message (module AuthenticationPath),
            not_packed )
        ^:: repeated ((2, "index", "index"), bool, packed)
        ^:: basic ((3, "rt", "rt"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { authentication_paths; index; rt } ->
        serialize writer authentication_paths index rt

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor authentication_paths index rt =
        { authentication_paths; index; rt }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { authentication_paths; index; rt } ->
        serialize authentication_paths index rt

    let from_json_exn =
      let constructor authentication_paths index rt =
        { authentication_paths; index; rt }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and OutputPoint : sig
    type t = { hash : bytes; index : int }

    val make : ?hash:bytes -> ?index:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?hash:bytes -> ?index:int -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = OutputPoint

    let name () = ".protocol.OutputPoint"

    type t = { hash : bytes; index : int }
    type make_t = ?hash:bytes -> ?index:int -> unit -> t

    let make ?(hash = Bytes.of_string {||}) ?(index = 0) () = { hash; index }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_hash =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((1, "hash", "hash"), bytes, Bytes.of_string {||}))
           in
           let merge_index =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((2, "index", "index"), int32_int, 0))
           in
           fun t1 t2 ->
             {
               hash = merge_hash t1.hash t2.hash;
               index = merge_index t1.index t2.index;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "hash", "hash"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "index", "index"), int32_int, 0)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { hash; index } -> serialize writer hash index

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor hash index = { hash; index } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { hash; index } -> serialize hash index

    let from_json_exn =
      let constructor hash index = { hash; index } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and OutputPointInfo : sig
    type t = { out_points : OutputPoint.t list; block_num : int }

    val make : ?out_points:OutputPoint.t list -> ?block_num:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?out_points:OutputPoint.t list -> ?block_num:int -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = OutputPointInfo

    let name () = ".protocol.OutputPointInfo"

    type t = { out_points : OutputPoint.t list; block_num : int }
    type make_t = ?out_points:OutputPoint.t list -> ?block_num:int -> unit -> t

    let make ?(out_points = []) ?(block_num = 0) () = { out_points; block_num }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_out_points =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (1, "out_points", "outPoints"),
                     message (module OutputPoint),
                     not_packed ))
           in
           let merge_block_num =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "block_num", "blockNum"), int32_int, 0))
           in
           fun t1 t2 ->
             {
               out_points = merge_out_points t1.out_points t2.out_points;
               block_num = merge_block_num t1.block_num t2.block_num;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        repeated
          ( (1, "out_points", "outPoints"),
            message (module OutputPoint),
            not_packed )
        ^:: basic ((2, "block_num", "blockNum"), int32_int, 0)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { out_points; block_num } ->
        serialize writer out_points block_num

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor out_points block_num = { out_points; block_num } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { out_points; block_num } -> serialize out_points block_num

    let from_json_exn =
      let constructor out_points block_num = { out_points; block_num } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and PedersenHash : sig
    type t = bytes

    val make : ?content:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?content:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = PedersenHash

    let name () = ".protocol.PedersenHash"

    type t = bytes
    type make_t = ?content:bytes -> unit -> t

    let make ?(content = Bytes.of_string {||}) () = content

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_content =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((1, "content", "content"), bytes, Bytes.of_string {||}))
           in
           fun t1_content t2_content -> merge_content t1_content t2_content)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "content", "content"), bytes, Bytes.of_string {||}) ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer content -> serialize writer content

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor content = content in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun content -> serialize content

    let from_json_exn =
      let constructor content = content in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and IncrementalMerkleTree : sig
    type t = {
      left : PedersenHash.t option;
      right : PedersenHash.t option;
      parents : PedersenHash.t list;
    }

    val make :
      ?left:PedersenHash.t ->
      ?right:PedersenHash.t ->
      ?parents:PedersenHash.t list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?left:PedersenHash.t ->
      ?right:PedersenHash.t ->
      ?parents:PedersenHash.t list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = IncrementalMerkleTree

    let name () = ".protocol.IncrementalMerkleTree"

    type t = {
      left : PedersenHash.t option;
      right : PedersenHash.t option;
      parents : PedersenHash.t list;
    }

    type make_t =
      ?left:PedersenHash.t ->
      ?right:PedersenHash.t ->
      ?parents:PedersenHash.t list ->
      unit ->
      t

    let make ?left ?right ?(parents = []) () = { left; right; parents }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_left =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((1, "left", "left"), message (module PedersenHash)))
           in
           let merge_right =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((2, "right", "right"), message (module PedersenHash)))
           in
           let merge_parents =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (3, "parents", "parents"),
                     message (module PedersenHash),
                     not_packed ))
           in
           fun t1 t2 ->
             {
               left = merge_left t1.left t2.left;
               right = merge_right t1.right t2.right;
               parents = merge_parents t1.parents t2.parents;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt ((1, "left", "left"), message (module PedersenHash))
        ^:: basic_opt ((2, "right", "right"), message (module PedersenHash))
        ^:: repeated
              ( (3, "parents", "parents"),
                message (module PedersenHash),
                not_packed )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { left; right; parents } -> serialize writer left right parents

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor left right parents = { left; right; parents } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { left; right; parents } -> serialize left right parents

    let from_json_exn =
      let constructor left right parents = { left; right; parents } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and IncrementalMerkleVoucher : sig
    type t = {
      tree : IncrementalMerkleTree.t option;
      filled : PedersenHash.t list;
      cursor : IncrementalMerkleTree.t option;
      cursor_depth : int64;
      rt : bytes;
      output_point : OutputPoint.t option;
    }

    val make :
      ?tree:IncrementalMerkleTree.t ->
      ?filled:PedersenHash.t list ->
      ?cursor:IncrementalMerkleTree.t ->
      ?cursor_depth:int64 ->
      ?rt:bytes ->
      ?output_point:OutputPoint.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?tree:IncrementalMerkleTree.t ->
      ?filled:PedersenHash.t list ->
      ?cursor:IncrementalMerkleTree.t ->
      ?cursor_depth:int64 ->
      ?rt:bytes ->
      ?output_point:OutputPoint.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = IncrementalMerkleVoucher

    let name () = ".protocol.IncrementalMerkleVoucher"

    type t = {
      tree : IncrementalMerkleTree.t option;
      filled : PedersenHash.t list;
      cursor : IncrementalMerkleTree.t option;
      cursor_depth : int64;
      rt : bytes;
      output_point : OutputPoint.t option;
    }

    type make_t =
      ?tree:IncrementalMerkleTree.t ->
      ?filled:PedersenHash.t list ->
      ?cursor:IncrementalMerkleTree.t ->
      ?cursor_depth:int64 ->
      ?rt:bytes ->
      ?output_point:OutputPoint.t ->
      unit ->
      t

    let make ?tree ?(filled = []) ?cursor ?(cursor_depth = 0L)
        ?(rt = Bytes.of_string {||}) ?output_point () =
      { tree; filled; cursor; cursor_depth; rt; output_point }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_tree =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ((1, "tree", "tree"), message (module IncrementalMerkleTree)))
           in
           let merge_filled =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (2, "filled", "filled"),
                     message (module PedersenHash),
                     not_packed ))
           in
           let merge_cursor =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (3, "cursor", "cursor"),
                     message (module IncrementalMerkleTree) ))
           in
           let merge_cursor_depth =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((4, "cursor_depth", "cursorDepth"), int64, 0L))
           in
           let merge_rt =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((5, "rt", "rt"), bytes, Bytes.of_string {||}))
           in
           let merge_output_point =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (10, "output_point", "outputPoint"),
                     message (module OutputPoint) ))
           in
           fun t1 t2 ->
             {
               tree = merge_tree t1.tree t2.tree;
               filled = merge_filled t1.filled t2.filled;
               cursor = merge_cursor t1.cursor t2.cursor;
               cursor_depth = merge_cursor_depth t1.cursor_depth t2.cursor_depth;
               rt = merge_rt t1.rt t2.rt;
               output_point = merge_output_point t1.output_point t2.output_point;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt ((1, "tree", "tree"), message (module IncrementalMerkleTree))
        ^:: repeated
              ( (2, "filled", "filled"),
                message (module PedersenHash),
                not_packed )
        ^:: basic_opt
              ((3, "cursor", "cursor"), message (module IncrementalMerkleTree))
        ^:: basic ((4, "cursor_depth", "cursorDepth"), int64, 0L)
        ^:: basic ((5, "rt", "rt"), bytes, Bytes.of_string {||})
        ^:: basic_opt
              ((10, "output_point", "outputPoint"), message (module OutputPoint))
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { tree; filled; cursor; cursor_depth; rt; output_point } ->
        serialize writer tree filled cursor cursor_depth rt output_point

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor tree filled cursor cursor_depth rt output_point =
        { tree; filled; cursor; cursor_depth; rt; output_point }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { tree; filled; cursor; cursor_depth; rt; output_point } ->
        serialize tree filled cursor cursor_depth rt output_point

    let from_json_exn =
      let constructor tree filled cursor cursor_depth rt output_point =
        { tree; filled; cursor; cursor_depth; rt; output_point }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and IncrementalMerkleVoucherInfo : sig
    type t = { vouchers : IncrementalMerkleVoucher.t list; paths : bytes list }

    val make :
      ?vouchers:IncrementalMerkleVoucher.t list ->
      ?paths:bytes list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?vouchers:IncrementalMerkleVoucher.t list ->
      ?paths:bytes list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = IncrementalMerkleVoucherInfo

    let name () = ".protocol.IncrementalMerkleVoucherInfo"

    type t = { vouchers : IncrementalMerkleVoucher.t list; paths : bytes list }

    type make_t =
      ?vouchers:IncrementalMerkleVoucher.t list ->
      ?paths:bytes list ->
      unit ->
      t

    let make ?(vouchers = []) ?(paths = []) () = { vouchers; paths }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_vouchers =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (1, "vouchers", "vouchers"),
                     message (module IncrementalMerkleVoucher),
                     not_packed ))
           in
           let merge_paths =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated ((2, "paths", "paths"), bytes, not_packed))
           in
           fun t1 t2 ->
             {
               vouchers = merge_vouchers t1.vouchers t2.vouchers;
               paths = merge_paths t1.paths t2.paths;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        repeated
          ( (1, "vouchers", "vouchers"),
            message (module IncrementalMerkleVoucher),
            not_packed )
        ^:: repeated ((2, "paths", "paths"), bytes, not_packed)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { vouchers; paths } -> serialize writer vouchers paths

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor vouchers paths = { vouchers; paths } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { vouchers; paths } -> serialize vouchers paths

    let from_json_exn =
      let constructor vouchers paths = { vouchers; paths } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SpendDescription : sig
    type t = {
      value_commitment : bytes;
      anchor : bytes;  (** {%html:
<p>merkle root</p>
%} *)
      nullifier : bytes;  (** {%html:
<p>used for check double spend</p>
%} *)
      rk : bytes;
          (** {%html:
<p>used for check spend authority signature</p>
%} *)
      zkproof : bytes;
      spend_authority_signature : bytes;
    }

    val make :
      ?value_commitment:bytes ->
      ?anchor:bytes ->
      ?nullifier:bytes ->
      ?rk:bytes ->
      ?zkproof:bytes ->
      ?spend_authority_signature:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?value_commitment:bytes ->
      ?anchor:bytes ->
      ?nullifier:bytes ->
      ?rk:bytes ->
      ?zkproof:bytes ->
      ?spend_authority_signature:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = SpendDescription

    let name () = ".protocol.SpendDescription"

    type t = {
      value_commitment : bytes;
      anchor : bytes;
      nullifier : bytes;
      rk : bytes;
      zkproof : bytes;
      spend_authority_signature : bytes;
    }

    type make_t =
      ?value_commitment:bytes ->
      ?anchor:bytes ->
      ?nullifier:bytes ->
      ?rk:bytes ->
      ?zkproof:bytes ->
      ?spend_authority_signature:bytes ->
      unit ->
      t

    let make ?(value_commitment = Bytes.of_string {||})
        ?(anchor = Bytes.of_string {||}) ?(nullifier = Bytes.of_string {||})
        ?(rk = Bytes.of_string {||}) ?(zkproof = Bytes.of_string {||})
        ?(spend_authority_signature = Bytes.of_string {||}) () =
      {
        value_commitment;
        anchor;
        nullifier;
        rk;
        zkproof;
        spend_authority_signature;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_value_commitment =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "value_commitment", "valueCommitment"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_anchor =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "anchor", "anchor"), bytes, Bytes.of_string {||}))
           in
           let merge_nullifier =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((3, "nullifier", "nullifier"), bytes, Bytes.of_string {||}))
           in
           let merge_rk =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((4, "rk", "rk"), bytes, Bytes.of_string {||}))
           in
           let merge_zkproof =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((5, "zkproof", "zkproof"), bytes, Bytes.of_string {||}))
           in
           let merge_spend_authority_signature =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (6, "spend_authority_signature", "spendAuthoritySignature"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               value_commitment =
                 merge_value_commitment t1.value_commitment t2.value_commitment;
               anchor = merge_anchor t1.anchor t2.anchor;
               nullifier = merge_nullifier t1.nullifier t2.nullifier;
               rk = merge_rk t1.rk t2.rk;
               zkproof = merge_zkproof t1.zkproof t2.zkproof;
               spend_authority_signature =
                 merge_spend_authority_signature t1.spend_authority_signature
                   t2.spend_authority_signature;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic
          ( (1, "value_commitment", "valueCommitment"),
            bytes,
            Bytes.of_string {||} )
        ^:: basic ((2, "anchor", "anchor"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "nullifier", "nullifier"), bytes, Bytes.of_string {||})
        ^:: basic ((4, "rk", "rk"), bytes, Bytes.of_string {||})
        ^:: basic ((5, "zkproof", "zkproof"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (6, "spend_authority_signature", "spendAuthoritySignature"),
                bytes,
                Bytes.of_string {||} )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          value_commitment;
          anchor;
          nullifier;
          rk;
          zkproof;
          spend_authority_signature;
        }
      ->
        serialize writer value_commitment anchor nullifier rk zkproof
          spend_authority_signature

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor value_commitment anchor nullifier rk zkproof
          spend_authority_signature =
        {
          value_commitment;
          anchor;
          nullifier;
          rk;
          zkproof;
          spend_authority_signature;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            value_commitment;
            anchor;
            nullifier;
            rk;
            zkproof;
            spend_authority_signature;
          }
      ->
        serialize value_commitment anchor nullifier rk zkproof
          spend_authority_signature

    let from_json_exn =
      let constructor value_commitment anchor nullifier rk zkproof
          spend_authority_signature =
        {
          value_commitment;
          anchor;
          nullifier;
          rk;
          zkproof;
          spend_authority_signature;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and ReceiveDescription : sig
    type t = {
      value_commitment : bytes;
      note_commitment : bytes;
      epk : bytes;  (** {%html:
<p>for Encryption</p>
%} *)
      c_enc : bytes;
          (** {%html:
<p>Encryption for incoming, decrypt it with ivk</p>
%} *)
      c_out : bytes;
          (** {%html:
<p>Encryption for audit, decrypt it with ovk</p>
%} *)
      zkproof : bytes;
    }

    val make :
      ?value_commitment:bytes ->
      ?note_commitment:bytes ->
      ?epk:bytes ->
      ?c_enc:bytes ->
      ?c_out:bytes ->
      ?zkproof:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?value_commitment:bytes ->
      ?note_commitment:bytes ->
      ?epk:bytes ->
      ?c_enc:bytes ->
      ?c_out:bytes ->
      ?zkproof:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ReceiveDescription

    let name () = ".protocol.ReceiveDescription"

    type t = {
      value_commitment : bytes;
      note_commitment : bytes;
      epk : bytes;
      c_enc : bytes;
      c_out : bytes;
      zkproof : bytes;
    }

    type make_t =
      ?value_commitment:bytes ->
      ?note_commitment:bytes ->
      ?epk:bytes ->
      ?c_enc:bytes ->
      ?c_out:bytes ->
      ?zkproof:bytes ->
      unit ->
      t

    let make ?(value_commitment = Bytes.of_string {||})
        ?(note_commitment = Bytes.of_string {||}) ?(epk = Bytes.of_string {||})
        ?(c_enc = Bytes.of_string {||}) ?(c_out = Bytes.of_string {||})
        ?(zkproof = Bytes.of_string {||}) () =
      { value_commitment; note_commitment; epk; c_enc; c_out; zkproof }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_value_commitment =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "value_commitment", "valueCommitment"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_note_commitment =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "note_commitment", "noteCommitment"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_epk =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "epk", "epk"), bytes, Bytes.of_string {||}))
           in
           let merge_c_enc =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((4, "c_enc", "cEnc"), bytes, Bytes.of_string {||}))
           in
           let merge_c_out =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((5, "c_out", "cOut"), bytes, Bytes.of_string {||}))
           in
           let merge_zkproof =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((6, "zkproof", "zkproof"), bytes, Bytes.of_string {||}))
           in
           fun t1 t2 ->
             {
               value_commitment =
                 merge_value_commitment t1.value_commitment t2.value_commitment;
               note_commitment =
                 merge_note_commitment t1.note_commitment t2.note_commitment;
               epk = merge_epk t1.epk t2.epk;
               c_enc = merge_c_enc t1.c_enc t2.c_enc;
               c_out = merge_c_out t1.c_out t2.c_out;
               zkproof = merge_zkproof t1.zkproof t2.zkproof;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic
          ( (1, "value_commitment", "valueCommitment"),
            bytes,
            Bytes.of_string {||} )
        ^:: basic
              ( (2, "note_commitment", "noteCommitment"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic ((3, "epk", "epk"), bytes, Bytes.of_string {||})
        ^:: basic ((4, "c_enc", "cEnc"), bytes, Bytes.of_string {||})
        ^:: basic ((5, "c_out", "cOut"), bytes, Bytes.of_string {||})
        ^:: basic ((6, "zkproof", "zkproof"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        { value_commitment; note_commitment; epk; c_enc; c_out; zkproof }
      ->
        serialize writer value_commitment note_commitment epk c_enc c_out
          zkproof

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor value_commitment note_commitment epk c_enc c_out zkproof =
        { value_commitment; note_commitment; epk; c_enc; c_out; zkproof }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { value_commitment; note_commitment; epk; c_enc; c_out; zkproof } ->
        serialize value_commitment note_commitment epk c_enc c_out zkproof

    let from_json_exn =
      let constructor value_commitment note_commitment epk c_enc c_out zkproof =
        { value_commitment; note_commitment; epk; c_enc; c_out; zkproof }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and ShieldedTransferContract : sig
    type t = {
      transparent_from_address : bytes;
          (** {%html:
<p>transparent address</p>
%} *)
      from_amount : int64;
      spend_description : SpendDescription.t list;
      receive_description : ReceiveDescription.t list;
      binding_signature : bytes;
      transparent_to_address : bytes;
          (** {%html:
<p>transparent address</p>
%} *)
      to_amount : int64;
          (** {%html:
<p>the amount to transparent to_address</p>
%} *)
    }

    val make :
      ?transparent_from_address:bytes ->
      ?from_amount:int64 ->
      ?spend_description:SpendDescription.t list ->
      ?receive_description:ReceiveDescription.t list ->
      ?binding_signature:bytes ->
      ?transparent_to_address:bytes ->
      ?to_amount:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?transparent_from_address:bytes ->
      ?from_amount:int64 ->
      ?spend_description:SpendDescription.t list ->
      ?receive_description:ReceiveDescription.t list ->
      ?binding_signature:bytes ->
      ?transparent_to_address:bytes ->
      ?to_amount:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ShieldedTransferContract

    let name () = ".protocol.ShieldedTransferContract"

    type t = {
      transparent_from_address : bytes;
      from_amount : int64;
      spend_description : SpendDescription.t list;
      receive_description : ReceiveDescription.t list;
      binding_signature : bytes;
      transparent_to_address : bytes;
      to_amount : int64;
    }

    type make_t =
      ?transparent_from_address:bytes ->
      ?from_amount:int64 ->
      ?spend_description:SpendDescription.t list ->
      ?receive_description:ReceiveDescription.t list ->
      ?binding_signature:bytes ->
      ?transparent_to_address:bytes ->
      ?to_amount:int64 ->
      unit ->
      t

    let make ?(transparent_from_address = Bytes.of_string {||})
        ?(from_amount = 0L) ?(spend_description = [])
        ?(receive_description = []) ?(binding_signature = Bytes.of_string {||})
        ?(transparent_to_address = Bytes.of_string {||}) ?(to_amount = 0L) () =
      {
        transparent_from_address;
        from_amount;
        spend_description;
        receive_description;
        binding_signature;
        transparent_to_address;
        to_amount;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_transparent_from_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "transparent_from_address", "transparentFromAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_from_amount =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "from_amount", "fromAmount"), int64, 0L))
           in
           let merge_spend_description =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (3, "spend_description", "spendDescription"),
                     message (module SpendDescription),
                     not_packed ))
           in
           let merge_receive_description =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (4, "receive_description", "receiveDescription"),
                     message (module ReceiveDescription),
                     not_packed ))
           in
           let merge_binding_signature =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (5, "binding_signature", "bindingSignature"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_transparent_to_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (6, "transparent_to_address", "transparentToAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_to_amount =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((7, "to_amount", "toAmount"), int64, 0L))
           in
           fun t1 t2 ->
             {
               transparent_from_address =
                 merge_transparent_from_address t1.transparent_from_address
                   t2.transparent_from_address;
               from_amount = merge_from_amount t1.from_amount t2.from_amount;
               spend_description =
                 merge_spend_description t1.spend_description
                   t2.spend_description;
               receive_description =
                 merge_receive_description t1.receive_description
                   t2.receive_description;
               binding_signature =
                 merge_binding_signature t1.binding_signature
                   t2.binding_signature;
               transparent_to_address =
                 merge_transparent_to_address t1.transparent_to_address
                   t2.transparent_to_address;
               to_amount = merge_to_amount t1.to_amount t2.to_amount;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic
          ( (1, "transparent_from_address", "transparentFromAddress"),
            bytes,
            Bytes.of_string {||} )
        ^:: basic ((2, "from_amount", "fromAmount"), int64, 0L)
        ^:: repeated
              ( (3, "spend_description", "spendDescription"),
                message (module SpendDescription),
                not_packed )
        ^:: repeated
              ( (4, "receive_description", "receiveDescription"),
                message (module ReceiveDescription),
                not_packed )
        ^:: basic
              ( (5, "binding_signature", "bindingSignature"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic
              ( (6, "transparent_to_address", "transparentToAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic ((7, "to_amount", "toAmount"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          transparent_from_address;
          from_amount;
          spend_description;
          receive_description;
          binding_signature;
          transparent_to_address;
          to_amount;
        }
      ->
        serialize writer transparent_from_address from_amount spend_description
          receive_description binding_signature transparent_to_address to_amount

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor transparent_from_address from_amount spend_description
          receive_description binding_signature transparent_to_address to_amount
          =
        {
          transparent_from_address;
          from_amount;
          spend_description;
          receive_description;
          binding_signature;
          transparent_to_address;
          to_amount;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            transparent_from_address;
            from_amount;
            spend_description;
            receive_description;
            binding_signature;
            transparent_to_address;
            to_amount;
          }
      ->
        serialize transparent_from_address from_amount spend_description
          receive_description binding_signature transparent_to_address to_amount

    let from_json_exn =
      let constructor transparent_from_address from_amount spend_description
          receive_description binding_signature transparent_to_address to_amount
          =
        {
          transparent_from_address;
          from_amount;
          spend_description;
          receive_description;
          binding_signature;
          transparent_to_address;
          to_amount;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
