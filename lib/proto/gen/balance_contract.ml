(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/balance_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Common = Common
end

(**/**)

module rec Protocol : sig
  module rec FreezeBalanceContract : sig
    type t = {
      owner_address : bytes;
      frozen_balance : int64;
      frozen_duration : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      receiver_address : bytes;
    }

    val make :
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?frozen_duration:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?frozen_duration:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UnfreezeBalanceContract : sig
    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      receiver_address : bytes;
    }

    val make :
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and WithdrawBalanceContract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and TransferContract : sig
    type t = { owner_address : bytes; to_address : bytes; amount : int64 }

    val make :
      ?owner_address:bytes -> ?to_address:bytes -> ?amount:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes -> ?to_address:bytes -> ?amount:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and TransactionBalanceTrace : sig
    module rec Operation : sig
      type t = { operation_identifier : int64; address : bytes; amount : int64 }

      val make :
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      transaction_identifier : bytes;
      operation : Operation.t list;
      type' : string;
      status : string;
    }

    val make :
      ?transaction_identifier:bytes ->
      ?operation:Operation.t list ->
      ?type':string ->
      ?status:string ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?transaction_identifier:bytes ->
      ?operation:Operation.t list ->
      ?type':string ->
      ?status:string ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and BlockBalanceTrace : sig
    module rec BlockIdentifier : sig
      type t = { hash : bytes; number : int64 }

      val make : ?hash:bytes -> ?number:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?hash:bytes -> ?number:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      block_identifier : BlockIdentifier.t option;
      timestamp : int64;
      transaction_balance_trace : TransactionBalanceTrace.t list;
          (** {%html:
<pre><code>BlockIdentifier parent_block_identifier = 4;
</code></pre>
%}
          *)
    }

    val make :
      ?block_identifier:BlockIdentifier.t ->
      ?timestamp:int64 ->
      ?transaction_balance_trace:TransactionBalanceTrace.t list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?block_identifier:BlockIdentifier.t ->
      ?timestamp:int64 ->
      ?transaction_balance_trace:TransactionBalanceTrace.t list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and AccountTrace : sig
    type t = { balance : int64; placeholder : int64 }

    val make : ?balance:int64 -> ?placeholder:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?balance:int64 -> ?placeholder:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and AccountIdentifier : sig
    type t = bytes

    val make : ?address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and AccountBalanceRequest : sig
    type t = {
      account_identifier : AccountIdentifier.t option;
      block_identifier : BlockBalanceTrace.BlockIdentifier.t option;
    }

    val make :
      ?account_identifier:AccountIdentifier.t ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?account_identifier:AccountIdentifier.t ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and AccountBalanceResponse : sig
    type t = {
      balance : int64;
      block_identifier : BlockBalanceTrace.BlockIdentifier.t option;
    }

    val make :
      ?balance:int64 ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?balance:int64 ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and FreezeBalanceV2Contract : sig
    type t = {
      owner_address : bytes;
      frozen_balance : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
    }

    val make :
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UnfreezeBalanceV2Contract : sig
    type t = {
      owner_address : bytes;
      unfreeze_balance : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
    }

    val make :
      ?owner_address:bytes ->
      ?unfreeze_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?unfreeze_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and WithdrawExpireUnfreezeContract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and DelegateResourceContract : sig
    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      balance : int64;
      receiver_address : bytes;
      lock : bool;
      lock_period : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      ?lock:bool ->
      ?lock_period:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      ?lock:bool ->
      ?lock_period:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and UnDelegateResourceContract : sig
    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      balance : int64;
      receiver_address : bytes;
    }

    val make :
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and CancelAllUnfreezeV2Contract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec FreezeBalanceContract : sig
    type t = {
      owner_address : bytes;
      frozen_balance : int64;
      frozen_duration : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      receiver_address : bytes;
    }

    val make :
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?frozen_duration:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?frozen_duration:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = FreezeBalanceContract

    let name () = ".protocol.FreezeBalanceContract"

    type t = {
      owner_address : bytes;
      frozen_balance : int64;
      frozen_duration : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      receiver_address : bytes;
    }

    type make_t =
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?frozen_duration:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||}) ?(frozen_balance = 0L)
        ?(frozen_duration = 0L)
        ?(resource =
          Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0)
        ?(receiver_address = Bytes.of_string {||}) () =
      {
        owner_address;
        frozen_balance;
        frozen_duration;
        resource;
        receiver_address;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_frozen_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "frozen_balance", "frozenBalance"), int64, 0L))
           in
           let merge_frozen_duration =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "frozen_duration", "frozenDuration"), int64, 0L))
           in
           let merge_resource =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (10, "resource", "resource"),
                     enum (module Imported'modules.Common.Protocol.ResourceCode),
                     Imported'modules.Common.Protocol.ResourceCode.from_int_exn
                       0 ))
           in
           let merge_receiver_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (15, "receiver_address", "receiverAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               frozen_balance =
                 merge_frozen_balance t1.frozen_balance t2.frozen_balance;
               frozen_duration =
                 merge_frozen_duration t1.frozen_duration t2.frozen_duration;
               resource = merge_resource t1.resource t2.resource;
               receiver_address =
                 merge_receiver_address t1.receiver_address t2.receiver_address;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "frozen_balance", "frozenBalance"), int64, 0L)
        ^:: basic ((3, "frozen_duration", "frozenDuration"), int64, 0L)
        ^:: basic
              ( (10, "resource", "resource"),
                enum (module Imported'modules.Common.Protocol.ResourceCode),
                Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0 )
        ^:: basic
              ( (15, "receiver_address", "receiverAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          owner_address;
          frozen_balance;
          frozen_duration;
          resource;
          receiver_address;
        }
      ->
        serialize writer owner_address frozen_balance frozen_duration resource
          receiver_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address frozen_balance frozen_duration resource
          receiver_address =
        {
          owner_address;
          frozen_balance;
          frozen_duration;
          resource;
          receiver_address;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            owner_address;
            frozen_balance;
            frozen_duration;
            resource;
            receiver_address;
          }
      ->
        serialize owner_address frozen_balance frozen_duration resource
          receiver_address

    let from_json_exn =
      let constructor owner_address frozen_balance frozen_duration resource
          receiver_address =
        {
          owner_address;
          frozen_balance;
          frozen_duration;
          resource;
          receiver_address;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UnfreezeBalanceContract : sig
    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      receiver_address : bytes;
    }

    val make :
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UnfreezeBalanceContract

    let name () = ".protocol.UnfreezeBalanceContract"

    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      receiver_address : bytes;
    }

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?receiver_address:bytes ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(resource =
          Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0)
        ?(receiver_address = Bytes.of_string {||}) () =
      { owner_address; resource; receiver_address }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_resource =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (10, "resource", "resource"),
                     enum (module Imported'modules.Common.Protocol.ResourceCode),
                     Imported'modules.Common.Protocol.ResourceCode.from_int_exn
                       0 ))
           in
           let merge_receiver_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (15, "receiver_address", "receiverAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               resource = merge_resource t1.resource t2.resource;
               receiver_address =
                 merge_receiver_address t1.receiver_address t2.receiver_address;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (10, "resource", "resource"),
                enum (module Imported'modules.Common.Protocol.ResourceCode),
                Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0 )
        ^:: basic
              ( (15, "receiver_address", "receiverAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; resource; receiver_address } ->
        serialize writer owner_address resource receiver_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address resource receiver_address =
        { owner_address; resource; receiver_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; resource; receiver_address } ->
        serialize owner_address resource receiver_address

    let from_json_exn =
      let constructor owner_address resource receiver_address =
        { owner_address; resource; receiver_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and WithdrawBalanceContract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = WithdrawBalanceContract

    let name () = ".protocol.WithdrawBalanceContract"

    type t = bytes
    type make_t = ?owner_address:bytes -> unit -> t

    let make ?(owner_address = Bytes.of_string {||}) () = owner_address

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1_owner_address t2_owner_address ->
             merge_owner_address t1_owner_address t2_owner_address)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer owner_address -> serialize writer owner_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun owner_address -> serialize owner_address

    let from_json_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and TransferContract : sig
    type t = { owner_address : bytes; to_address : bytes; amount : int64 }

    val make :
      ?owner_address:bytes -> ?to_address:bytes -> ?amount:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes -> ?to_address:bytes -> ?amount:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = TransferContract

    let name () = ".protocol.TransferContract"

    type t = { owner_address : bytes; to_address : bytes; amount : int64 }

    type make_t =
      ?owner_address:bytes -> ?to_address:bytes -> ?amount:int64 -> unit -> t

    let make ?(owner_address = Bytes.of_string {||})
        ?(to_address = Bytes.of_string {||}) ?(amount = 0L) () =
      { owner_address; to_address; amount }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_to_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((2, "to_address", "toAddress"), bytes, Bytes.of_string {||}))
           in
           let merge_amount =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "amount", "amount"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               to_address = merge_to_address t1.to_address t2.to_address;
               amount = merge_amount t1.amount t2.amount;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "to_address", "toAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "amount", "amount"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; to_address; amount } ->
        serialize writer owner_address to_address amount

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address to_address amount =
        { owner_address; to_address; amount }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; to_address; amount } ->
        serialize owner_address to_address amount

    let from_json_exn =
      let constructor owner_address to_address amount =
        { owner_address; to_address; amount }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and TransactionBalanceTrace : sig
    module rec Operation : sig
      type t = { operation_identifier : int64; address : bytes; amount : int64 }

      val make :
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      transaction_identifier : bytes;
      operation : Operation.t list;
      type' : string;
      status : string;
    }

    val make :
      ?transaction_identifier:bytes ->
      ?operation:Operation.t list ->
      ?type':string ->
      ?status:string ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?transaction_identifier:bytes ->
      ?operation:Operation.t list ->
      ?type':string ->
      ?status:string ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = TransactionBalanceTrace

    module rec Operation : sig
      type t = { operation_identifier : int64; address : bytes; amount : int64 }

      val make :
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Operation

      let name () = ".protocol.TransactionBalanceTrace.Operation"

      type t = { operation_identifier : int64; address : bytes; amount : int64 }

      type make_t =
        ?operation_identifier:int64 ->
        ?address:bytes ->
        ?amount:int64 ->
        unit ->
        t

      let make ?(operation_identifier = 0L) ?(address = Bytes.of_string {||})
          ?(amount = 0L) () =
        { operation_identifier; address; amount }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_operation_identifier =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (1, "operation_identifier", "operationIdentifier"),
                       int64,
                       0L ))
             in
             let merge_address =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "address", "address"), bytes, Bytes.of_string {||}))
             in
             let merge_amount =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((3, "amount", "amount"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 operation_identifier =
                   merge_operation_identifier t1.operation_identifier
                     t2.operation_identifier;
                 address = merge_address t1.address t2.address;
                 amount = merge_amount t1.amount t2.amount;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "operation_identifier", "operationIdentifier"), int64, 0L)
          ^:: basic ((2, "address", "address"), bytes, Bytes.of_string {||})
          ^:: basic ((3, "amount", "amount"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { operation_identifier; address; amount } ->
          serialize writer operation_identifier address amount

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor operation_identifier address amount =
          { operation_identifier; address; amount }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { operation_identifier; address; amount } ->
          serialize operation_identifier address amount

      let from_json_exn =
        let constructor operation_identifier address amount =
          { operation_identifier; address; amount }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    let name () = ".protocol.TransactionBalanceTrace"

    type t = {
      transaction_identifier : bytes;
      operation : Operation.t list;
      type' : string;
      status : string;
    }

    type make_t =
      ?transaction_identifier:bytes ->
      ?operation:Operation.t list ->
      ?type':string ->
      ?status:string ->
      unit ->
      t

    let make ?(transaction_identifier = Bytes.of_string {||}) ?(operation = [])
        ?(type' = {||}) ?(status = {||}) () =
      { transaction_identifier; operation; type'; status }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_transaction_identifier =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "transaction_identifier", "transactionIdentifier"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_operation =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (2, "operation", "operation"),
                     message (module Operation),
                     not_packed ))
           in
           let merge_type' =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "type", "type"), string, {||}))
           in
           let merge_status =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "status", "status"), string, {||}))
           in
           fun t1 t2 ->
             {
               transaction_identifier =
                 merge_transaction_identifier t1.transaction_identifier
                   t2.transaction_identifier;
               operation = merge_operation t1.operation t2.operation;
               type' = merge_type' t1.type' t2.type';
               status = merge_status t1.status t2.status;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic
          ( (1, "transaction_identifier", "transactionIdentifier"),
            bytes,
            Bytes.of_string {||} )
        ^:: repeated
              ( (2, "operation", "operation"),
                message (module Operation),
                not_packed )
        ^:: basic ((3, "type", "type"), string, {||})
        ^:: basic ((4, "status", "status"), string, {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { transaction_identifier; operation; type'; status } ->
        serialize writer transaction_identifier operation type' status

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor transaction_identifier operation type' status =
        { transaction_identifier; operation; type'; status }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { transaction_identifier; operation; type'; status } ->
        serialize transaction_identifier operation type' status

    let from_json_exn =
      let constructor transaction_identifier operation type' status =
        { transaction_identifier; operation; type'; status }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and BlockBalanceTrace : sig
    module rec BlockIdentifier : sig
      type t = { hash : bytes; number : int64 }

      val make : ?hash:bytes -> ?number:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?hash:bytes -> ?number:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    type t = {
      block_identifier : BlockIdentifier.t option;
      timestamp : int64;
      transaction_balance_trace : TransactionBalanceTrace.t list;
          (** {%html:
<pre><code>BlockIdentifier parent_block_identifier = 4;
</code></pre>
%}
          *)
    }

    val make :
      ?block_identifier:BlockIdentifier.t ->
      ?timestamp:int64 ->
      ?transaction_balance_trace:TransactionBalanceTrace.t list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?block_identifier:BlockIdentifier.t ->
      ?timestamp:int64 ->
      ?transaction_balance_trace:TransactionBalanceTrace.t list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = BlockBalanceTrace

    module rec BlockIdentifier : sig
      type t = { hash : bytes; number : int64 }

      val make : ?hash:bytes -> ?number:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?hash:bytes -> ?number:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = BlockIdentifier

      let name () = ".protocol.BlockBalanceTrace.BlockIdentifier"

      type t = { hash : bytes; number : int64 }
      type make_t = ?hash:bytes -> ?number:int64 -> unit -> t

      let make ?(hash = Bytes.of_string {||}) ?(number = 0L) () =
        { hash; number }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((1, "hash", "hash"), bytes, Bytes.of_string {||}))
             in
             let merge_number =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "number", "number"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 hash = merge_hash t1.hash t2.hash;
                 number = merge_number t1.number t2.number;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "hash", "hash"), bytes, Bytes.of_string {||})
          ^:: basic ((2, "number", "number"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { hash; number } -> serialize writer hash number

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor hash number = { hash; number } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { hash; number } -> serialize hash number

      let from_json_exn =
        let constructor hash number = { hash; number } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    let name () = ".protocol.BlockBalanceTrace"

    type t = {
      block_identifier : BlockIdentifier.t option;
      timestamp : int64;
      transaction_balance_trace : TransactionBalanceTrace.t list;
    }

    type make_t =
      ?block_identifier:BlockIdentifier.t ->
      ?timestamp:int64 ->
      ?transaction_balance_trace:TransactionBalanceTrace.t list ->
      unit ->
      t

    let make ?block_identifier ?(timestamp = 0L)
        ?(transaction_balance_trace = []) () =
      { block_identifier; timestamp; transaction_balance_trace }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_block_identifier =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (1, "block_identifier", "blockIdentifier"),
                     message (module BlockIdentifier) ))
           in
           let merge_timestamp =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((2, "timestamp", "timestamp"), int64, 0L))
           in
           let merge_transaction_balance_trace =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (3, "transaction_balance_trace", "transactionBalanceTrace"),
                     message (module TransactionBalanceTrace),
                     not_packed ))
           in
           fun t1 t2 ->
             {
               block_identifier =
                 merge_block_identifier t1.block_identifier t2.block_identifier;
               timestamp = merge_timestamp t1.timestamp t2.timestamp;
               transaction_balance_trace =
                 merge_transaction_balance_trace t1.transaction_balance_trace
                   t2.transaction_balance_trace;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt
          ( (1, "block_identifier", "blockIdentifier"),
            message (module BlockIdentifier) )
        ^:: basic ((2, "timestamp", "timestamp"), int64, 0L)
        ^:: repeated
              ( (3, "transaction_balance_trace", "transactionBalanceTrace"),
                message (module TransactionBalanceTrace),
                not_packed )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { block_identifier; timestamp; transaction_balance_trace } ->
        serialize writer block_identifier timestamp transaction_balance_trace

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor block_identifier timestamp transaction_balance_trace =
        { block_identifier; timestamp; transaction_balance_trace }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { block_identifier; timestamp; transaction_balance_trace } ->
        serialize block_identifier timestamp transaction_balance_trace

    let from_json_exn =
      let constructor block_identifier timestamp transaction_balance_trace =
        { block_identifier; timestamp; transaction_balance_trace }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and AccountTrace : sig
    type t = { balance : int64; placeholder : int64 }

    val make : ?balance:int64 -> ?placeholder:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?balance:int64 -> ?placeholder:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountTrace

    let name () = ".protocol.AccountTrace"

    type t = { balance : int64; placeholder : int64 }
    type make_t = ?balance:int64 -> ?placeholder:int64 -> unit -> t

    let make ?(balance = 0L) ?(placeholder = 0L) () = { balance; placeholder }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((1, "balance", "balance"), int64, 0L))
           in
           let merge_placeholder =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((99, "placeholder", "placeholder"), int64, 0L))
           in
           fun t1 t2 ->
             {
               balance = merge_balance t1.balance t2.balance;
               placeholder = merge_placeholder t1.placeholder t2.placeholder;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "balance", "balance"), int64, 0L)
        ^:: basic ((99, "placeholder", "placeholder"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { balance; placeholder } ->
        serialize writer balance placeholder

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor balance placeholder = { balance; placeholder } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { balance; placeholder } -> serialize balance placeholder

    let from_json_exn =
      let constructor balance placeholder = { balance; placeholder } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and AccountIdentifier : sig
    type t = bytes

    val make : ?address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountIdentifier

    let name () = ".protocol.AccountIdentifier"

    type t = bytes
    type make_t = ?address:bytes -> unit -> t

    let make ?(address = Bytes.of_string {||}) () = address

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((1, "address", "address"), bytes, Bytes.of_string {||}))
           in
           fun t1_address t2_address -> merge_address t1_address t2_address)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "address", "address"), bytes, Bytes.of_string {||}) ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer address -> serialize writer address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor address = address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun address -> serialize address

    let from_json_exn =
      let constructor address = address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and AccountBalanceRequest : sig
    type t = {
      account_identifier : AccountIdentifier.t option;
      block_identifier : BlockBalanceTrace.BlockIdentifier.t option;
    }

    val make :
      ?account_identifier:AccountIdentifier.t ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?account_identifier:AccountIdentifier.t ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountBalanceRequest

    let name () = ".protocol.AccountBalanceRequest"

    type t = {
      account_identifier : AccountIdentifier.t option;
      block_identifier : BlockBalanceTrace.BlockIdentifier.t option;
    }

    type make_t =
      ?account_identifier:AccountIdentifier.t ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t

    let make ?account_identifier ?block_identifier () =
      { account_identifier; block_identifier }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_account_identifier =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (1, "account_identifier", "accountIdentifier"),
                     message (module AccountIdentifier) ))
           in
           let merge_block_identifier =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (2, "block_identifier", "blockIdentifier"),
                     message (module BlockBalanceTrace.BlockIdentifier) ))
           in
           fun t1 t2 ->
             {
               account_identifier =
                 merge_account_identifier t1.account_identifier
                   t2.account_identifier;
               block_identifier =
                 merge_block_identifier t1.block_identifier t2.block_identifier;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt
          ( (1, "account_identifier", "accountIdentifier"),
            message (module AccountIdentifier) )
        ^:: basic_opt
              ( (2, "block_identifier", "blockIdentifier"),
                message (module BlockBalanceTrace.BlockIdentifier) )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { account_identifier; block_identifier } ->
        serialize writer account_identifier block_identifier

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor account_identifier block_identifier =
        { account_identifier; block_identifier }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { account_identifier; block_identifier } ->
        serialize account_identifier block_identifier

    let from_json_exn =
      let constructor account_identifier block_identifier =
        { account_identifier; block_identifier }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and AccountBalanceResponse : sig
    type t = {
      balance : int64;
      block_identifier : BlockBalanceTrace.BlockIdentifier.t option;
    }

    val make :
      ?balance:int64 ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?balance:int64 ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountBalanceResponse

    let name () = ".protocol.AccountBalanceResponse"

    type t = {
      balance : int64;
      block_identifier : BlockBalanceTrace.BlockIdentifier.t option;
    }

    type make_t =
      ?balance:int64 ->
      ?block_identifier:BlockBalanceTrace.BlockIdentifier.t ->
      unit ->
      t

    let make ?(balance = 0L) ?block_identifier () =
      { balance; block_identifier }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((1, "balance", "balance"), int64, 0L))
           in
           let merge_block_identifier =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (2, "block_identifier", "blockIdentifier"),
                     message (module BlockBalanceTrace.BlockIdentifier) ))
           in
           fun t1 t2 ->
             {
               balance = merge_balance t1.balance t2.balance;
               block_identifier =
                 merge_block_identifier t1.block_identifier t2.block_identifier;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "balance", "balance"), int64, 0L)
        ^:: basic_opt
              ( (2, "block_identifier", "blockIdentifier"),
                message (module BlockBalanceTrace.BlockIdentifier) )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { balance; block_identifier } ->
        serialize writer balance block_identifier

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor balance block_identifier =
        { balance; block_identifier }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { balance; block_identifier } -> serialize balance block_identifier

    let from_json_exn =
      let constructor balance block_identifier =
        { balance; block_identifier }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and FreezeBalanceV2Contract : sig
    type t = {
      owner_address : bytes;
      frozen_balance : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
    }

    val make :
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = FreezeBalanceV2Contract

    let name () = ".protocol.FreezeBalanceV2Contract"

    type t = {
      owner_address : bytes;
      frozen_balance : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
    }

    type make_t =
      ?owner_address:bytes ->
      ?frozen_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||}) ?(frozen_balance = 0L)
        ?(resource =
          Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0) () =
      { owner_address; frozen_balance; resource }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_frozen_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "frozen_balance", "frozenBalance"), int64, 0L))
           in
           let merge_resource =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (3, "resource", "resource"),
                     enum (module Imported'modules.Common.Protocol.ResourceCode),
                     Imported'modules.Common.Protocol.ResourceCode.from_int_exn
                       0 ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               frozen_balance =
                 merge_frozen_balance t1.frozen_balance t2.frozen_balance;
               resource = merge_resource t1.resource t2.resource;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "frozen_balance", "frozenBalance"), int64, 0L)
        ^:: basic
              ( (3, "resource", "resource"),
                enum (module Imported'modules.Common.Protocol.ResourceCode),
                Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0 )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; frozen_balance; resource } ->
        serialize writer owner_address frozen_balance resource

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address frozen_balance resource =
        { owner_address; frozen_balance; resource }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; frozen_balance; resource } ->
        serialize owner_address frozen_balance resource

    let from_json_exn =
      let constructor owner_address frozen_balance resource =
        { owner_address; frozen_balance; resource }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UnfreezeBalanceV2Contract : sig
    type t = {
      owner_address : bytes;
      unfreeze_balance : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
    }

    val make :
      ?owner_address:bytes ->
      ?unfreeze_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?unfreeze_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UnfreezeBalanceV2Contract

    let name () = ".protocol.UnfreezeBalanceV2Contract"

    type t = {
      owner_address : bytes;
      unfreeze_balance : int64;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
    }

    type make_t =
      ?owner_address:bytes ->
      ?unfreeze_balance:int64 ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||}) ?(unfreeze_balance = 0L)
        ?(resource =
          Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0) () =
      { owner_address; unfreeze_balance; resource }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_unfreeze_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "unfreeze_balance", "unfreezeBalance"), int64, 0L))
           in
           let merge_resource =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (3, "resource", "resource"),
                     enum (module Imported'modules.Common.Protocol.ResourceCode),
                     Imported'modules.Common.Protocol.ResourceCode.from_int_exn
                       0 ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               unfreeze_balance =
                 merge_unfreeze_balance t1.unfreeze_balance t2.unfreeze_balance;
               resource = merge_resource t1.resource t2.resource;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "unfreeze_balance", "unfreezeBalance"), int64, 0L)
        ^:: basic
              ( (3, "resource", "resource"),
                enum (module Imported'modules.Common.Protocol.ResourceCode),
                Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0 )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; unfreeze_balance; resource } ->
        serialize writer owner_address unfreeze_balance resource

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address unfreeze_balance resource =
        { owner_address; unfreeze_balance; resource }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; unfreeze_balance; resource } ->
        serialize owner_address unfreeze_balance resource

    let from_json_exn =
      let constructor owner_address unfreeze_balance resource =
        { owner_address; unfreeze_balance; resource }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and WithdrawExpireUnfreezeContract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = WithdrawExpireUnfreezeContract

    let name () = ".protocol.WithdrawExpireUnfreezeContract"

    type t = bytes
    type make_t = ?owner_address:bytes -> unit -> t

    let make ?(owner_address = Bytes.of_string {||}) () = owner_address

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1_owner_address t2_owner_address ->
             merge_owner_address t1_owner_address t2_owner_address)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer owner_address -> serialize writer owner_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun owner_address -> serialize owner_address

    let from_json_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and DelegateResourceContract : sig
    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      balance : int64;
      receiver_address : bytes;
      lock : bool;
      lock_period : int64;
    }

    val make :
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      ?lock:bool ->
      ?lock_period:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      ?lock:bool ->
      ?lock_period:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = DelegateResourceContract

    let name () = ".protocol.DelegateResourceContract"

    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      balance : int64;
      receiver_address : bytes;
      lock : bool;
      lock_period : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      ?lock:bool ->
      ?lock_period:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(resource =
          Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0)
        ?(balance = 0L) ?(receiver_address = Bytes.of_string {||})
        ?(lock = false) ?(lock_period = 0L) () =
      { owner_address; resource; balance; receiver_address; lock; lock_period }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_resource =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "resource", "resource"),
                     enum (module Imported'modules.Common.Protocol.ResourceCode),
                     Imported'modules.Common.Protocol.ResourceCode.from_int_exn
                       0 ))
           in
           let merge_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "balance", "balance"), int64, 0L))
           in
           let merge_receiver_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (4, "receiver_address", "receiverAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_lock =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((5, "lock", "lock"), bool, false))
           in
           let merge_lock_period =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((6, "lock_period", "lockPeriod"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               resource = merge_resource t1.resource t2.resource;
               balance = merge_balance t1.balance t2.balance;
               receiver_address =
                 merge_receiver_address t1.receiver_address t2.receiver_address;
               lock = merge_lock t1.lock t2.lock;
               lock_period = merge_lock_period t1.lock_period t2.lock_period;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "resource", "resource"),
                enum (module Imported'modules.Common.Protocol.ResourceCode),
                Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0 )
        ^:: basic ((3, "balance", "balance"), int64, 0L)
        ^:: basic
              ( (4, "receiver_address", "receiverAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic ((5, "lock", "lock"), bool, false)
        ^:: basic ((6, "lock_period", "lockPeriod"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          owner_address;
          resource;
          balance;
          receiver_address;
          lock;
          lock_period;
        }
      ->
        serialize writer owner_address resource balance receiver_address lock
          lock_period

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address resource balance receiver_address lock
          lock_period =
        {
          owner_address;
          resource;
          balance;
          receiver_address;
          lock;
          lock_period;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            owner_address;
            resource;
            balance;
            receiver_address;
            lock;
            lock_period;
          }
      ->
        serialize owner_address resource balance receiver_address lock
          lock_period

    let from_json_exn =
      let constructor owner_address resource balance receiver_address lock
          lock_period =
        {
          owner_address;
          resource;
          balance;
          receiver_address;
          lock;
          lock_period;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and UnDelegateResourceContract : sig
    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      balance : int64;
      receiver_address : bytes;
    }

    val make :
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = UnDelegateResourceContract

    let name () = ".protocol.UnDelegateResourceContract"

    type t = {
      owner_address : bytes;
      resource : Imported'modules.Common.Protocol.ResourceCode.t;
      balance : int64;
      receiver_address : bytes;
    }

    type make_t =
      ?owner_address:bytes ->
      ?resource:Imported'modules.Common.Protocol.ResourceCode.t ->
      ?balance:int64 ->
      ?receiver_address:bytes ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(resource =
          Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0)
        ?(balance = 0L) ?(receiver_address = Bytes.of_string {||}) () =
      { owner_address; resource; balance; receiver_address }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_resource =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "resource", "resource"),
                     enum (module Imported'modules.Common.Protocol.ResourceCode),
                     Imported'modules.Common.Protocol.ResourceCode.from_int_exn
                       0 ))
           in
           let merge_balance =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "balance", "balance"), int64, 0L))
           in
           let merge_receiver_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (4, "receiver_address", "receiverAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               resource = merge_resource t1.resource t2.resource;
               balance = merge_balance t1.balance t2.balance;
               receiver_address =
                 merge_receiver_address t1.receiver_address t2.receiver_address;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "resource", "resource"),
                enum (module Imported'modules.Common.Protocol.ResourceCode),
                Imported'modules.Common.Protocol.ResourceCode.from_int_exn 0 )
        ^:: basic ((3, "balance", "balance"), int64, 0L)
        ^:: basic
              ( (4, "receiver_address", "receiverAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; resource; balance; receiver_address } ->
        serialize writer owner_address resource balance receiver_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address resource balance receiver_address =
        { owner_address; resource; balance; receiver_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; resource; balance; receiver_address } ->
        serialize owner_address resource balance receiver_address

    let from_json_exn =
      let constructor owner_address resource balance receiver_address =
        { owner_address; resource; balance; receiver_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and CancelAllUnfreezeV2Contract : sig
    type t = bytes

    val make : ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = CancelAllUnfreezeV2Contract

    let name () = ".protocol.CancelAllUnfreezeV2Contract"

    type t = bytes
    type make_t = ?owner_address:bytes -> unit -> t

    let make ?(owner_address = Bytes.of_string {||}) () = owner_address

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1_owner_address t2_owner_address ->
             merge_owner_address t1_owner_address t2_owner_address)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer owner_address -> serialize writer owner_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun owner_address -> serialize owner_address

    let from_json_exn =
      let constructor owner_address = owner_address in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
