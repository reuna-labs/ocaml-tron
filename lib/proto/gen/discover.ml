(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/Discover.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct end

(**/**)

module rec Protocol : sig
  module rec Endpoint : sig
    type t = {
      address : bytes;
      port : int;
      nodeId : bytes;
      addressIpv6 : bytes;
    }

    val make :
      ?address:bytes ->
      ?port:int ->
      ?nodeId:bytes ->
      ?addressIpv6:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?address:bytes ->
      ?port:int ->
      ?nodeId:bytes ->
      ?addressIpv6:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and PingMessage : sig
    type t = {
      from : Endpoint.t option;
      to' : Endpoint.t option;
      version : int;
      timestamp : int64;
    }

    val make :
      ?from:Endpoint.t ->
      ?to':Endpoint.t ->
      ?version:int ->
      ?timestamp:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?from:Endpoint.t ->
      ?to':Endpoint.t ->
      ?version:int ->
      ?timestamp:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and PongMessage : sig
    type t = { from : Endpoint.t option; echo : int; timestamp : int64 }

    val make : ?from:Endpoint.t -> ?echo:int -> ?timestamp:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?from:Endpoint.t -> ?echo:int -> ?timestamp:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and FindNeighbours : sig
    type t = { from : Endpoint.t option; targetId : bytes; timestamp : int64 }

    val make :
      ?from:Endpoint.t -> ?targetId:bytes -> ?timestamp:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?from:Endpoint.t -> ?targetId:bytes -> ?timestamp:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and Neighbours : sig
    type t = {
      from : Endpoint.t option;
      neighbours : Endpoint.t list;
      timestamp : int64;
    }

    val make :
      ?from:Endpoint.t ->
      ?neighbours:Endpoint.t list ->
      ?timestamp:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?from:Endpoint.t ->
      ?neighbours:Endpoint.t list ->
      ?timestamp:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and BackupMessage : sig
    type t = { flag : bool; priority : int }

    val make : ?flag:bool -> ?priority:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?flag:bool -> ?priority:int -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec Endpoint : sig
    type t = {
      address : bytes;
      port : int;
      nodeId : bytes;
      addressIpv6 : bytes;
    }

    val make :
      ?address:bytes ->
      ?port:int ->
      ?nodeId:bytes ->
      ?addressIpv6:bytes ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?address:bytes ->
      ?port:int ->
      ?nodeId:bytes ->
      ?addressIpv6:bytes ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = Endpoint

    let name () = ".protocol.Endpoint"

    type t = {
      address : bytes;
      port : int;
      nodeId : bytes;
      addressIpv6 : bytes;
    }

    type make_t =
      ?address:bytes ->
      ?port:int ->
      ?nodeId:bytes ->
      ?addressIpv6:bytes ->
      unit ->
      t

    let make ?(address = Bytes.of_string {||}) ?(port = 0)
        ?(nodeId = Bytes.of_string {||}) ?(addressIpv6 = Bytes.of_string {||})
        () =
      { address; port; nodeId; addressIpv6 }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((1, "address", "address"), bytes, Bytes.of_string {||}))
           in
           let merge_port =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((2, "port", "port"), int32_int, 0))
           in
           let merge_nodeId =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "nodeId", "nodeId"), bytes, Bytes.of_string {||}))
           in
           let merge_addressIpv6 =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (4, "addressIpv6", "addressIpv6"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               address = merge_address t1.address t2.address;
               port = merge_port t1.port t2.port;
               nodeId = merge_nodeId t1.nodeId t2.nodeId;
               addressIpv6 = merge_addressIpv6 t1.addressIpv6 t2.addressIpv6;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "address", "address"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "port", "port"), int32_int, 0)
        ^:: basic ((3, "nodeId", "nodeId"), bytes, Bytes.of_string {||})
        ^:: basic
              ((4, "addressIpv6", "addressIpv6"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { address; port; nodeId; addressIpv6 } ->
        serialize writer address port nodeId addressIpv6

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor address port nodeId addressIpv6 =
        { address; port; nodeId; addressIpv6 }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { address; port; nodeId; addressIpv6 } ->
        serialize address port nodeId addressIpv6

    let from_json_exn =
      let constructor address port nodeId addressIpv6 =
        { address; port; nodeId; addressIpv6 }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and PingMessage : sig
    type t = {
      from : Endpoint.t option;
      to' : Endpoint.t option;
      version : int;
      timestamp : int64;
    }

    val make :
      ?from:Endpoint.t ->
      ?to':Endpoint.t ->
      ?version:int ->
      ?timestamp:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?from:Endpoint.t ->
      ?to':Endpoint.t ->
      ?version:int ->
      ?timestamp:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = PingMessage

    let name () = ".protocol.PingMessage"

    type t = {
      from : Endpoint.t option;
      to' : Endpoint.t option;
      version : int;
      timestamp : int64;
    }

    type make_t =
      ?from:Endpoint.t ->
      ?to':Endpoint.t ->
      ?version:int ->
      ?timestamp:int64 ->
      unit ->
      t

    let make ?from ?to' ?(version = 0) ?(timestamp = 0L) () =
      { from; to'; version; timestamp }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_from =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((1, "from", "from"), message (module Endpoint)))
           in
           let merge_to' =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((2, "to", "to"), message (module Endpoint)))
           in
           let merge_version =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "version", "version"), int32_int, 0))
           in
           let merge_timestamp =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "timestamp", "timestamp"), int64, 0L))
           in
           fun t1 t2 ->
             {
               from = merge_from t1.from t2.from;
               to' = merge_to' t1.to' t2.to';
               version = merge_version t1.version t2.version;
               timestamp = merge_timestamp t1.timestamp t2.timestamp;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt ((1, "from", "from"), message (module Endpoint))
        ^:: basic_opt ((2, "to", "to"), message (module Endpoint))
        ^:: basic ((3, "version", "version"), int32_int, 0)
        ^:: basic ((4, "timestamp", "timestamp"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { from; to'; version; timestamp } ->
        serialize writer from to' version timestamp

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor from to' version timestamp =
        { from; to'; version; timestamp }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { from; to'; version; timestamp } ->
        serialize from to' version timestamp

    let from_json_exn =
      let constructor from to' version timestamp =
        { from; to'; version; timestamp }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and PongMessage : sig
    type t = { from : Endpoint.t option; echo : int; timestamp : int64 }

    val make : ?from:Endpoint.t -> ?echo:int -> ?timestamp:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?from:Endpoint.t -> ?echo:int -> ?timestamp:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = PongMessage

    let name () = ".protocol.PongMessage"

    type t = { from : Endpoint.t option; echo : int; timestamp : int64 }
    type make_t = ?from:Endpoint.t -> ?echo:int -> ?timestamp:int64 -> unit -> t

    let make ?from ?(echo = 0) ?(timestamp = 0L) () = { from; echo; timestamp }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_from =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((1, "from", "from"), message (module Endpoint)))
           in
           let merge_echo =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((2, "echo", "echo"), int32_int, 0))
           in
           let merge_timestamp =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "timestamp", "timestamp"), int64, 0L))
           in
           fun t1 t2 ->
             {
               from = merge_from t1.from t2.from;
               echo = merge_echo t1.echo t2.echo;
               timestamp = merge_timestamp t1.timestamp t2.timestamp;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt ((1, "from", "from"), message (module Endpoint))
        ^:: basic ((2, "echo", "echo"), int32_int, 0)
        ^:: basic ((3, "timestamp", "timestamp"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { from; echo; timestamp } ->
        serialize writer from echo timestamp

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor from echo timestamp = { from; echo; timestamp } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { from; echo; timestamp } -> serialize from echo timestamp

    let from_json_exn =
      let constructor from echo timestamp = { from; echo; timestamp } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and FindNeighbours : sig
    type t = { from : Endpoint.t option; targetId : bytes; timestamp : int64 }

    val make :
      ?from:Endpoint.t -> ?targetId:bytes -> ?timestamp:int64 -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?from:Endpoint.t -> ?targetId:bytes -> ?timestamp:int64 -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = FindNeighbours

    let name () = ".protocol.FindNeighbours"

    type t = { from : Endpoint.t option; targetId : bytes; timestamp : int64 }

    type make_t =
      ?from:Endpoint.t -> ?targetId:bytes -> ?timestamp:int64 -> unit -> t

    let make ?from ?(targetId = Bytes.of_string {||}) ?(timestamp = 0L) () =
      { from; targetId; timestamp }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_from =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((1, "from", "from"), message (module Endpoint)))
           in
           let merge_targetId =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "targetId", "targetId"), bytes, Bytes.of_string {||}))
           in
           let merge_timestamp =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "timestamp", "timestamp"), int64, 0L))
           in
           fun t1 t2 ->
             {
               from = merge_from t1.from t2.from;
               targetId = merge_targetId t1.targetId t2.targetId;
               timestamp = merge_timestamp t1.timestamp t2.timestamp;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt ((1, "from", "from"), message (module Endpoint))
        ^:: basic ((2, "targetId", "targetId"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "timestamp", "timestamp"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { from; targetId; timestamp } ->
        serialize writer from targetId timestamp

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor from targetId timestamp = { from; targetId; timestamp } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { from; targetId; timestamp } -> serialize from targetId timestamp

    let from_json_exn =
      let constructor from targetId timestamp = { from; targetId; timestamp } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and Neighbours : sig
    type t = {
      from : Endpoint.t option;
      neighbours : Endpoint.t list;
      timestamp : int64;
    }

    val make :
      ?from:Endpoint.t ->
      ?neighbours:Endpoint.t list ->
      ?timestamp:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?from:Endpoint.t ->
      ?neighbours:Endpoint.t list ->
      ?timestamp:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = Neighbours

    let name () = ".protocol.Neighbours"

    type t = {
      from : Endpoint.t option;
      neighbours : Endpoint.t list;
      timestamp : int64;
    }

    type make_t =
      ?from:Endpoint.t ->
      ?neighbours:Endpoint.t list ->
      ?timestamp:int64 ->
      unit ->
      t

    let make ?from ?(neighbours = []) ?(timestamp = 0L) () =
      { from; neighbours; timestamp }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_from =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt ((1, "from", "from"), message (module Endpoint)))
           in
           let merge_neighbours =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (2, "neighbours", "neighbours"),
                     message (module Endpoint),
                     not_packed ))
           in
           let merge_timestamp =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((3, "timestamp", "timestamp"), int64, 0L))
           in
           fun t1 t2 ->
             {
               from = merge_from t1.from t2.from;
               neighbours = merge_neighbours t1.neighbours t2.neighbours;
               timestamp = merge_timestamp t1.timestamp t2.timestamp;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt ((1, "from", "from"), message (module Endpoint))
        ^:: repeated
              ( (2, "neighbours", "neighbours"),
                message (module Endpoint),
                not_packed )
        ^:: basic ((3, "timestamp", "timestamp"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { from; neighbours; timestamp } ->
        serialize writer from neighbours timestamp

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor from neighbours timestamp =
        { from; neighbours; timestamp }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { from; neighbours; timestamp } -> serialize from neighbours timestamp

    let from_json_exn =
      let constructor from neighbours timestamp =
        { from; neighbours; timestamp }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and BackupMessage : sig
    type t = { flag : bool; priority : int }

    val make : ?flag:bool -> ?priority:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?flag:bool -> ?priority:int -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = BackupMessage

    let name () = ".protocol.BackupMessage"

    type t = { flag : bool; priority : int }
    type make_t = ?flag:bool -> ?priority:int -> unit -> t

    let make ?(flag = false) ?(priority = 0) () = { flag; priority }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_flag =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((1, "flag", "flag"), bool, false))
           in
           let merge_priority =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((2, "priority", "priority"), int32_int, 0))
           in
           fun t1 t2 ->
             {
               flag = merge_flag t1.flag t2.flag;
               priority = merge_priority t1.priority t2.priority;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "flag", "flag"), bool, false)
        ^:: basic ((2, "priority", "priority"), int32_int, 0)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { flag; priority } -> serialize writer flag priority

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor flag priority = { flag; priority } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { flag; priority } -> serialize flag priority

    let from_json_exn =
      let constructor flag priority = { flag; priority } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
