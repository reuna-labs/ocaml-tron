(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/market_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct end

(**/**)

module rec Protocol : sig
  module rec MarketSellAssetContract : sig
    type t = {
      owner_address : bytes;
      sell_token_id : bytes;
      sell_token_quantity : int64;
      buy_token_id : bytes;
      buy_token_quantity : int64;  (** {%html:
<p>min to receive</p>
%} *)
    }

    val make :
      ?owner_address:bytes ->
      ?sell_token_id:bytes ->
      ?sell_token_quantity:int64 ->
      ?buy_token_id:bytes ->
      ?buy_token_quantity:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?sell_token_id:bytes ->
      ?sell_token_quantity:int64 ->
      ?buy_token_id:bytes ->
      ?buy_token_quantity:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and MarketCancelOrderContract : sig
    type t = { owner_address : bytes; order_id : bytes }

    val make : ?owner_address:bytes -> ?order_id:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> ?order_id:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec MarketSellAssetContract : sig
    type t = {
      owner_address : bytes;
      sell_token_id : bytes;
      sell_token_quantity : int64;
      buy_token_id : bytes;
      buy_token_quantity : int64;  (** {%html:
<p>min to receive</p>
%} *)
    }

    val make :
      ?owner_address:bytes ->
      ?sell_token_id:bytes ->
      ?sell_token_quantity:int64 ->
      ?buy_token_id:bytes ->
      ?buy_token_quantity:int64 ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?sell_token_id:bytes ->
      ?sell_token_quantity:int64 ->
      ?buy_token_id:bytes ->
      ?buy_token_quantity:int64 ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = MarketSellAssetContract

    let name () = ".protocol.MarketSellAssetContract"

    type t = {
      owner_address : bytes;
      sell_token_id : bytes;
      sell_token_quantity : int64;
      buy_token_id : bytes;
      buy_token_quantity : int64;
    }

    type make_t =
      ?owner_address:bytes ->
      ?sell_token_id:bytes ->
      ?sell_token_quantity:int64 ->
      ?buy_token_id:bytes ->
      ?buy_token_quantity:int64 ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(sell_token_id = Bytes.of_string {||}) ?(sell_token_quantity = 0L)
        ?(buy_token_id = Bytes.of_string {||}) ?(buy_token_quantity = 0L) () =
      {
        owner_address;
        sell_token_id;
        sell_token_quantity;
        buy_token_id;
        buy_token_quantity;
      }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_sell_token_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "sell_token_id", "sellTokenId"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_sell_token_quantity =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((3, "sell_token_quantity", "sellTokenQuantity"), int64, 0L))
           in
           let merge_buy_token_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (4, "buy_token_id", "buyTokenId"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_buy_token_quantity =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((5, "buy_token_quantity", "buyTokenQuantity"), int64, 0L))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               sell_token_id =
                 merge_sell_token_id t1.sell_token_id t2.sell_token_id;
               sell_token_quantity =
                 merge_sell_token_quantity t1.sell_token_quantity
                   t2.sell_token_quantity;
               buy_token_id = merge_buy_token_id t1.buy_token_id t2.buy_token_id;
               buy_token_quantity =
                 merge_buy_token_quantity t1.buy_token_quantity
                   t2.buy_token_quantity;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ((2, "sell_token_id", "sellTokenId"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "sell_token_quantity", "sellTokenQuantity"), int64, 0L)
        ^:: basic
              ((4, "buy_token_id", "buyTokenId"), bytes, Bytes.of_string {||})
        ^:: basic ((5, "buy_token_quantity", "buyTokenQuantity"), int64, 0L)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer
        {
          owner_address;
          sell_token_id;
          sell_token_quantity;
          buy_token_id;
          buy_token_quantity;
        }
      ->
        serialize writer owner_address sell_token_id sell_token_quantity
          buy_token_id buy_token_quantity

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address sell_token_id sell_token_quantity
          buy_token_id buy_token_quantity =
        {
          owner_address;
          sell_token_id;
          sell_token_quantity;
          buy_token_id;
          buy_token_quantity;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun {
            owner_address;
            sell_token_id;
            sell_token_quantity;
            buy_token_id;
            buy_token_quantity;
          }
      ->
        serialize owner_address sell_token_id sell_token_quantity buy_token_id
          buy_token_quantity

    let from_json_exn =
      let constructor owner_address sell_token_id sell_token_quantity
          buy_token_id buy_token_quantity =
        {
          owner_address;
          sell_token_id;
          sell_token_quantity;
          buy_token_id;
          buy_token_quantity;
        }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and MarketCancelOrderContract : sig
    type t = { owner_address : bytes; order_id : bytes }

    val make : ?owner_address:bytes -> ?order_id:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?owner_address:bytes -> ?order_id:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = MarketCancelOrderContract

    let name () = ".protocol.MarketCancelOrderContract"

    type t = { owner_address : bytes; order_id : bytes }
    type make_t = ?owner_address:bytes -> ?order_id:bytes -> unit -> t

    let make ?(owner_address = Bytes.of_string {||})
        ?(order_id = Bytes.of_string {||}) () =
      { owner_address; order_id }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_order_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "order_id", "orderId"), bytes, Bytes.of_string {||}))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               order_id = merge_order_id t1.order_id t2.order_id;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic ((2, "order_id", "orderId"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; order_id } ->
        serialize writer owner_address order_id

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address order_id = { owner_address; order_id } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; order_id } -> serialize owner_address order_id

    let from_json_exn =
      let constructor owner_address order_id = { owner_address; order_id } in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
