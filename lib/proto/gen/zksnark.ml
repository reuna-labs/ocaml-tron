(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: api/zksnark.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Tron = Tron
end

(**/**)

module rec Protocol : sig
  module rec ZksnarkRequest : sig
    type t = {
      transaction : Imported'modules.Tron.Protocol.Transaction.t option;
      sighash : bytes;
      valueBalance : int64;
      txId : string;
    }

    val make :
      ?transaction:Imported'modules.Tron.Protocol.Transaction.t ->
      ?sighash:bytes ->
      ?valueBalance:int64 ->
      ?txId:string ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?transaction:Imported'modules.Tron.Protocol.Transaction.t ->
      ?sighash:bytes ->
      ?valueBalance:int64 ->
      ?txId:string ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and ZksnarkResponse : sig
    module rec Code : sig
      type t = SUCCESS | FAILED

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end

    type t = Code.t

    val make : ?code:Code.t -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?code:Code.t -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  module TronZksnark : sig
    module CheckZksnarkProof : sig
      include
        Runtime'.Service.Rpc
          with type Request.t = ZksnarkRequest.t
           and type Response.t = ZksnarkResponse.t

      (** Module alias for the request message for this method call *)
      module Request :
        Runtime'.Spec.Message
          with type t = ZksnarkRequest.t
           and type make_t = ZksnarkRequest.make_t

      (** Module alias for the response message for this method call *)
      module Response :
        Runtime'.Spec.Message
          with type t = ZksnarkResponse.t
           and type make_t = ZksnarkResponse.make_t
    end

    val checkZksnarkProof :
      (module Runtime'.Spec.Message with type t = ZksnarkRequest.t)
      * (module Runtime'.Spec.Message with type t = ZksnarkResponse.t)
  end
end = struct
  module rec ZksnarkRequest : sig
    type t = {
      transaction : Imported'modules.Tron.Protocol.Transaction.t option;
      sighash : bytes;
      valueBalance : int64;
      txId : string;
    }

    val make :
      ?transaction:Imported'modules.Tron.Protocol.Transaction.t ->
      ?sighash:bytes ->
      ?valueBalance:int64 ->
      ?txId:string ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?transaction:Imported'modules.Tron.Protocol.Transaction.t ->
      ?sighash:bytes ->
      ?valueBalance:int64 ->
      ?txId:string ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ZksnarkRequest

    let name () = ".protocol.ZksnarkRequest"

    type t = {
      transaction : Imported'modules.Tron.Protocol.Transaction.t option;
      sighash : bytes;
      valueBalance : int64;
      txId : string;
    }

    type make_t =
      ?transaction:Imported'modules.Tron.Protocol.Transaction.t ->
      ?sighash:bytes ->
      ?valueBalance:int64 ->
      ?txId:string ->
      unit ->
      t

    let make ?transaction ?(sighash = Bytes.of_string {||}) ?(valueBalance = 0L)
        ?(txId = {||}) () =
      { transaction; sighash; valueBalance; txId }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_transaction =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (1, "transaction", "transaction"),
                     message (module Imported'modules.Tron.Protocol.Transaction)
                   ))
           in
           let merge_sighash =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((2, "sighash", "sighash"), bytes, Bytes.of_string {||}))
           in
           let merge_valueBalance =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic ((3, "valueBalance", "valueBalance"), int64, 0L))
           in
           let merge_txId =
             Runtime'.Merge.merge
               Runtime'.Spec.(basic ((4, "txId", "txId"), string, {||}))
           in
           fun t1 t2 ->
             {
               transaction = merge_transaction t1.transaction t2.transaction;
               sighash = merge_sighash t1.sighash t2.sighash;
               valueBalance = merge_valueBalance t1.valueBalance t2.valueBalance;
               txId = merge_txId t1.txId t2.txId;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic_opt
          ( (1, "transaction", "transaction"),
            message (module Imported'modules.Tron.Protocol.Transaction) )
        ^:: basic ((2, "sighash", "sighash"), bytes, Bytes.of_string {||})
        ^:: basic ((3, "valueBalance", "valueBalance"), int64, 0L)
        ^:: basic ((4, "txId", "txId"), string, {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { transaction; sighash; valueBalance; txId } ->
        serialize writer transaction sighash valueBalance txId

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor transaction sighash valueBalance txId =
        { transaction; sighash; valueBalance; txId }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { transaction; sighash; valueBalance; txId } ->
        serialize transaction sighash valueBalance txId

    let from_json_exn =
      let constructor transaction sighash valueBalance txId =
        { transaction; sighash; valueBalance; txId }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and ZksnarkResponse : sig
    module rec Code : sig
      type t = SUCCESS | FAILED

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end

    type t = Code.t

    val make : ?code:Code.t -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?code:Code.t -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = ZksnarkResponse

    module rec Code : sig
      type t = SUCCESS | FAILED

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end = struct
      module This'_ = Code

      type t = SUCCESS | FAILED

      let name () = ".protocol.ZksnarkResponse.Code"
      let to_int = function SUCCESS -> 0 | FAILED -> 1

      let from_int_exn = function
        | 0 -> SUCCESS
        | 1 -> FAILED
        | n -> Runtime'.Result.raise (`Unknown_enum_value n)

      let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)
      let to_string = function SUCCESS -> "SUCCESS" | FAILED -> "FAILED"

      let from_string_exn = function
        | "SUCCESS" -> SUCCESS
        | "FAILED" -> FAILED
        | s -> Runtime'.Result.raise (`Unknown_enum_name s)
    end

    let name () = ".protocol.ZksnarkResponse"

    type t = Code.t
    type make_t = ?code:Code.t -> unit -> t

    let make ?(code = Code.from_int_exn 0) () = code

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_code =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((1, "code", "code"), enum (module Code), Code.from_int_exn 0))
           in
           fun t1_code t2_code -> merge_code t1_code t2_code)
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "code", "code"), enum (module Code), Code.from_int_exn 0)
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer code -> serialize writer code

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor code = code in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun code -> serialize code

    let from_json_exn =
      let constructor code = code in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  module TronZksnark = struct
    module CheckZksnarkProof = struct
      let package_name = Some "protocol"
      let service_name = "TronZksnark"
      let method_name = "CheckZksnarkProof"
      let name = "/protocol.TronZksnark/CheckZksnarkProof"

      module Request = ZksnarkRequest
      module Response = ZksnarkResponse
    end

    let checkZksnarkProof =
      ( (module ZksnarkRequest : Runtime'.Spec.Message
          with type t = ZksnarkRequest.t),
        (module ZksnarkResponse : Runtime'.Spec.Message
          with type t = ZksnarkResponse.t) )
  end
end
