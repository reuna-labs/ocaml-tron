(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: core/contract/account_contract.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Tron = Tron
end

(**/**)

module rec Protocol : sig
  module rec AccountCreateContract : sig
    type t = {
      owner_address : bytes;
      account_address : bytes;
      type' : Imported'modules.Tron.Protocol.AccountType.t;
    }

    val make :
      ?owner_address:bytes ->
      ?account_address:bytes ->
      ?type':Imported'modules.Tron.Protocol.AccountType.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?account_address:bytes ->
      ?type':Imported'modules.Tron.Protocol.AccountType.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  (** {%html:
<p>Update account name. Account name is not unique now.</p>
%} *)
  and AccountUpdateContract : sig
    type t = { account_name : bytes; owner_address : bytes }

    val make : ?account_name:bytes -> ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?account_name:bytes -> ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  (** {%html:
<p>Set account id if the account has no id. Account id is unique and case insensitive.</p>
%}
  *)
  and SetAccountIdContract : sig
    type t = { account_id : bytes; owner_address : bytes }

    val make : ?account_id:bytes -> ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?account_id:bytes -> ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end

  and AccountPermissionUpdateContract : sig
    type t = {
      owner_address : bytes;
      owner : Imported'modules.Tron.Protocol.Permission.t option;
          (** {%html:
<p>Empty is invalidate</p>
%} *)
      witness : Imported'modules.Tron.Protocol.Permission.t option;
          (** {%html:
<p>Can be empty</p>
%} *)
      actives : Imported'modules.Tron.Protocol.Permission.t list;
          (** {%html:
<p>Empty is invalidate</p>
%} *)
    }

    val make :
      ?owner_address:bytes ->
      ?owner:Imported'modules.Tron.Protocol.Permission.t ->
      ?witness:Imported'modules.Tron.Protocol.Permission.t ->
      ?actives:Imported'modules.Tron.Protocol.Permission.t list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?owner:Imported'modules.Tron.Protocol.Permission.t ->
      ?witness:Imported'modules.Tron.Protocol.Permission.t ->
      ?actives:Imported'modules.Tron.Protocol.Permission.t list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end
end = struct
  module rec AccountCreateContract : sig
    type t = {
      owner_address : bytes;
      account_address : bytes;
      type' : Imported'modules.Tron.Protocol.AccountType.t;
    }

    val make :
      ?owner_address:bytes ->
      ?account_address:bytes ->
      ?type':Imported'modules.Tron.Protocol.AccountType.t ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?account_address:bytes ->
      ?type':Imported'modules.Tron.Protocol.AccountType.t ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountCreateContract

    let name () = ".protocol.AccountCreateContract"

    type t = {
      owner_address : bytes;
      account_address : bytes;
      type' : Imported'modules.Tron.Protocol.AccountType.t;
    }

    type make_t =
      ?owner_address:bytes ->
      ?account_address:bytes ->
      ?type':Imported'modules.Tron.Protocol.AccountType.t ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||})
        ?(account_address = Bytes.of_string {||})
        ?(type' = Imported'modules.Tron.Protocol.AccountType.from_int_exn 0) ()
        =
      { owner_address; account_address; type' }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_account_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "account_address", "accountAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_type' =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (3, "type", "type"),
                     enum (module Imported'modules.Tron.Protocol.AccountType),
                     Imported'modules.Tron.Protocol.AccountType.from_int_exn 0
                   ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               account_address =
                 merge_account_address t1.account_address t2.account_address;
               type' = merge_type' t1.type' t2.type';
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic
              ( (2, "account_address", "accountAddress"),
                bytes,
                Bytes.of_string {||} )
        ^:: basic
              ( (3, "type", "type"),
                enum (module Imported'modules.Tron.Protocol.AccountType),
                Imported'modules.Tron.Protocol.AccountType.from_int_exn 0 )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; account_address; type' } ->
        serialize writer owner_address account_address type'

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address account_address type' =
        { owner_address; account_address; type' }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; account_address; type' } ->
        serialize owner_address account_address type'

    let from_json_exn =
      let constructor owner_address account_address type' =
        { owner_address; account_address; type' }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and AccountUpdateContract : sig
    type t = { account_name : bytes; owner_address : bytes }

    val make : ?account_name:bytes -> ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?account_name:bytes -> ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountUpdateContract

    let name () = ".protocol.AccountUpdateContract"

    type t = { account_name : bytes; owner_address : bytes }
    type make_t = ?account_name:bytes -> ?owner_address:bytes -> unit -> t

    let make ?(account_name = Bytes.of_string {||})
        ?(owner_address = Bytes.of_string {||}) () =
      { account_name; owner_address }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_account_name =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "account_name", "accountName"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               account_name = merge_account_name t1.account_name t2.account_name;
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "account_name", "accountName"), bytes, Bytes.of_string {||})
        ^:: basic
              ((2, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { account_name; owner_address } ->
        serialize writer account_name owner_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor account_name owner_address =
        { account_name; owner_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { account_name; owner_address } ->
        serialize account_name owner_address

    let from_json_exn =
      let constructor account_name owner_address =
        { account_name; owner_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SetAccountIdContract : sig
    type t = { account_id : bytes; owner_address : bytes }

    val make : ?account_id:bytes -> ?owner_address:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t = ?account_id:bytes -> ?owner_address:bytes -> unit -> t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = SetAccountIdContract

    let name () = ".protocol.SetAccountIdContract"

    type t = { account_id : bytes; owner_address : bytes }
    type make_t = ?account_id:bytes -> ?owner_address:bytes -> unit -> t

    let make ?(account_id = Bytes.of_string {||})
        ?(owner_address = Bytes.of_string {||}) () =
      { account_id; owner_address }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_account_id =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ((1, "account_id", "accountId"), bytes, Bytes.of_string {||}))
           in
           let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (2, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           fun t1 t2 ->
             {
               account_id = merge_account_id t1.account_id t2.account_id;
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "account_id", "accountId"), bytes, Bytes.of_string {||})
        ^:: basic
              ((2, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { account_id; owner_address } ->
        serialize writer account_id owner_address

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor account_id owner_address =
        { account_id; owner_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { account_id; owner_address } -> serialize account_id owner_address

    let from_json_exn =
      let constructor account_id owner_address =
        { account_id; owner_address }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and AccountPermissionUpdateContract : sig
    type t = {
      owner_address : bytes;
      owner : Imported'modules.Tron.Protocol.Permission.t option;
          (** {%html:
<p>Empty is invalidate</p>
%} *)
      witness : Imported'modules.Tron.Protocol.Permission.t option;
          (** {%html:
<p>Can be empty</p>
%} *)
      actives : Imported'modules.Tron.Protocol.Permission.t list;
          (** {%html:
<p>Empty is invalidate</p>
%} *)
    }

    val make :
      ?owner_address:bytes ->
      ?owner:Imported'modules.Tron.Protocol.Permission.t ->
      ?witness:Imported'modules.Tron.Protocol.Permission.t ->
      ?actives:Imported'modules.Tron.Protocol.Permission.t list ->
      unit ->
      t
    (** Helper function to generate a message using default values *)

    val to_proto : t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto : Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from binary format *)

    val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name : unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)

    type make_t =
      ?owner_address:bytes ->
      ?owner:Imported'modules.Tron.Protocol.Permission.t ->
      ?witness:Imported'modules.Tron.Protocol.Permission.t ->
      ?actives:Imported'modules.Tron.Protocol.Permission.t list ->
      unit ->
      t

    val merge : t -> t -> t
    val to_proto' : Runtime'.Writer.t -> t -> unit
    val from_proto_exn : Runtime'.Reader.t -> t
    val from_json_exn : Runtime'.Json.t -> t

    (**/**)
  end = struct
    module This'_ = AccountPermissionUpdateContract

    let name () = ".protocol.AccountPermissionUpdateContract"

    type t = {
      owner_address : bytes;
      owner : Imported'modules.Tron.Protocol.Permission.t option;
      witness : Imported'modules.Tron.Protocol.Permission.t option;
      actives : Imported'modules.Tron.Protocol.Permission.t list;
    }

    type make_t =
      ?owner_address:bytes ->
      ?owner:Imported'modules.Tron.Protocol.Permission.t ->
      ?witness:Imported'modules.Tron.Protocol.Permission.t ->
      ?actives:Imported'modules.Tron.Protocol.Permission.t list ->
      unit ->
      t

    let make ?(owner_address = Bytes.of_string {||}) ?owner ?witness
        ?(actives = []) () =
      { owner_address; owner; witness; actives }

    let merge =
      (* deferred by tools/lazify-merge.py; see that file *)
      let merge' =
        lazy
          (let merge_owner_address =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic
                   ( (1, "owner_address", "ownerAddress"),
                     bytes,
                     Bytes.of_string {||} ))
           in
           let merge_owner =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (2, "owner", "owner"),
                     message (module Imported'modules.Tron.Protocol.Permission)
                   ))
           in
           let merge_witness =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 basic_opt
                   ( (3, "witness", "witness"),
                     message (module Imported'modules.Tron.Protocol.Permission)
                   ))
           in
           let merge_actives =
             Runtime'.Merge.merge
               Runtime'.Spec.(
                 repeated
                   ( (4, "actives", "actives"),
                     message (module Imported'modules.Tron.Protocol.Permission),
                     not_packed ))
           in
           fun t1 t2 ->
             {
               owner_address =
                 merge_owner_address t1.owner_address t2.owner_address;
               owner = merge_owner t1.owner t2.owner;
               witness = merge_witness t1.witness t2.witness;
               actives = merge_actives t1.actives t2.actives;
             })
      in
      fun t1' t2' -> (Lazy.force merge') t1' t2'

    let spec () =
      Runtime'.Spec.(
        basic ((1, "owner_address", "ownerAddress"), bytes, Bytes.of_string {||})
        ^:: basic_opt
              ( (2, "owner", "owner"),
                message (module Imported'modules.Tron.Protocol.Permission) )
        ^:: basic_opt
              ( (3, "witness", "witness"),
                message (module Imported'modules.Tron.Protocol.Permission) )
        ^:: repeated
              ( (4, "actives", "actives"),
                message (module Imported'modules.Tron.Protocol.Permission),
                not_packed )
        ^:: nil)

    let to_proto' =
      let serialize =
        Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
      in
      fun writer { owner_address; owner; witness; actives } ->
        serialize writer owner_address owner witness actives

    let to_proto t =
      let writer = Runtime'.Writer.init () in
      to_proto' writer t;
      writer

    let from_proto_exn =
      let constructor owner_address owner witness actives =
        { owner_address; owner; witness; actives }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize.deserialize (spec ()) constructor)

    let from_proto writer =
      Runtime'.Result.catch (fun () -> from_proto_exn writer)

    let to_json options =
      let serialize =
        Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
          options
      in
      fun { owner_address; owner; witness; actives } ->
        serialize owner_address owner witness actives

    let from_json_exn =
      let constructor owner_address owner witness actives =
        { owner_address; owner; witness; actives }
      in
      Runtime'.apply_lazy (fun () ->
          Runtime'.Deserialize_json.deserialize ~message_name:(name ())
            (spec ()) constructor)

    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end
end
